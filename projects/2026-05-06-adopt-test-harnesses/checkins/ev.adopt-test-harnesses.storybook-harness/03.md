# Checkin 03 — ev.adopt-test-harnesses.storybook-harness

**Created**: 2026-05-06 17:42
**Phase**: 2 — Storybook harness
**Unit**: D3 — One vitest + RTL test for Stack (proves React testing harness works on real components)

## Contract

- **Goal**: Author one vitest + React Testing Library test against `Stack`
  to prove the React testing harness works for real components — not just
  pure utilities like Phase 1's `wrapResponsive`. This is the closing
  smoke screen for Phase 2: D1 installed Storybook, D2 wrote the first
  story, D3 lands the first React component test. Coverage expansion is
  out of scope; one focused test is the deliverable.

- **Acceptance criteria**:
  - File location: `components/shared/Stack/Stack.test.tsx` (colocated
    with the component; vitest's default glob picks up
    `**/*.{test,spec}.?(c|m)[jt]s?(x)`)
  - Test uses vitest's `test` and `expect` (or vitest globals if the
    config exposes them — current config does not, so explicit imports
    required)
  - Test uses `@testing-library/react` for rendering
  - Test uses `@testing-library/jest-dom/vitest` matchers (already
    auto-loaded via `vitest.setup.ts` from Phase 1 D1)
  - Exactly one `test(...)` block — single focused assertion
  - The assertion proves something Stack-specific that requires a
    rendering React harness (not something a pure-function test could
    do): rendering children inside the element configured by the `as`
    prop is the simplest meaningful smoke
  - `npm test` runs and the new test passes (24 tests total: 3
    wrapResponsive + 1 Stack + 20 substrate)
  - `npm run test:agentic` still passes (unchanged: 20 substrate tests)
  - `npm run lint` (Biome) clean — test file doesn't violate Biome rules
  - `npm run build` (Next.js) clean — test file is excluded from the
    Next.js build by convention (Next.js doesn't compile `.test.tsx`)
  - `npm run build-storybook` clean — Storybook's stories glob
    (`**/*.stories.@(tsx|mdx)`) does not match `.test.tsx`, so the test
    file should not appear as a story
  - No changes to `Stack` itself
  - No changes to `Stack.stories.tsx` (D2's artifact is read-only here)

- **Rules applied**:
  - Project CLAUDE.md: simple, no over-engineering, basic
  - User CLAUDE.md: tests prove real user functionality, not rote
    fundamentals — render-and-find-an-element is the right level of
    "real" for a smoke test on a layout primitive
  - User CLAUDE.md: prefer Cypress when possible, but vitest + RTL is
    the chosen tool for unit-flavored React tests in this project
  - Verification: `npm run lint`, `npm run build`, `npm test`,
    `npm run test:agentic`, `npm run build-storybook`

- **Disqualifiers**:
  - Adding multiple test cases (PLAN.md says "one vitest + RTL test
    against Stack" — singular)
  - Testing Stack's responsive prop behavior, which would require
    breakpoint mocking and is bigger than a smoke screen
  - Asserting on internal CSS module class names (brittle, tests
    implementation detail rather than user-observable behavior)
  - Asserting on computed styles via `getComputedStyle` in jsdom
    (jsdom doesn't compute styles from external stylesheets — the
    assertion would be fiction)
  - Modifying `Stack`, `Stack.module.css`, `Stack/types.ts`, or
    `Stack.stories.tsx` (all read-only here)
  - Adding new test infrastructure: custom matchers, custom renders,
    helper utilities. The smoke screen uses vanilla RTL
  - Adding `vitest.config.ts` changes (defaults are correct)
  - Adding `vitest.setup.ts` changes (jest-dom is already wired from
    Phase 1)

- **Inputs**:
  - `components/shared/Stack/Stack.test.tsx` (new)
  - `components/shared/Stack/index.tsx` (read-only)
  - `components/shared/Stack/types.ts` (read-only, prop types)
  - `vitest.setup.ts` (read-only — already imports jest-dom)
  - `vitest.config.ts` (read-only — environment: jsdom is already set)

- **Outputs**:
  - `components/shared/Stack/Stack.test.tsx` (~10-15 lines)
  - No other files changed

## Execution

Single new file: `components/shared/Stack/Stack.test.tsx` (10 lines).

```tsx
import { render, screen } from '@testing-library/react';
import { expect, test } from 'vitest';

import { Stack } from './index';

test('Stack renders children inside the element configured by the `as` prop', () => {
  render(
    <Stack as="section">
      <span>child-content</span>
    </Stack>,
  );

  const section = screen.getByText('child-content').closest('section');
  expect(section).not.toBeNull();
});
```

The single assertion `expect(section).not.toBeNull()` proves three things at once:
1. `render()` did not throw — the React testing harness mounts Stack
2. `screen.getByText('child-content')` found the child — Stack renders
   children
3. `.closest('section')` returned an element — Stack used `as="section"`
   to set the wrapper element

This is the smallest test that actually exercises @testing-library/react's
mounting + querying surface. Pure-function tests like Phase 1's
`wrapResponsive` could not produce equivalent evidence.

Used vanilla RTL (`render`, `screen`) and explicit vitest imports
(`test`, `expect`) — no custom matchers, no test helpers, no setup
file changes. The jest-dom matchers from `vitest.setup.ts` (Phase 1)
are available but this assertion happens to use plain `not.toBeNull()`,
which keeps the test self-explanatory.

Verification:

- `npm run lint` → 2 pre-existing symlink warnings, otherwise clean
- `npm test` → 3 files, 24 tests pass (3 wrapResponsive + 1 Stack + 20 substrate)
- `npm run test:agentic` → 1 file, 20 tests pass (substrate filter unchanged)
- `npm run build-storybook` → clean; `storybook-static/index.json`
  contains only `shared-stack--default` (the test file was not picked
  up by the stories glob)
- `npm run build` → clean (Next.js excludes `.test.tsx` from page
  collection by convention)

## Scope

- `components/shared/Stack/Stack.test.tsx` (new, 14 lines)

Out of scope (intentionally not touched):
- `components/shared/Stack/index.tsx` (read-only, the component
  under test)
- `components/shared/Stack/types.ts` (read-only)
- `components/shared/Stack/Stack.module.css` (read-only)
- `components/shared/Stack/Stack.stories.tsx` (D2's artifact, read-only here)
- `vitest.config.ts` (no changes — defaults work)
- `vitest.setup.ts` (no changes — jest-dom already wired in Phase 1)

## Changes since previous checkin

01 (D1) installed Storybook + framework + CSF Factories; 02 (D2) wrote
the first Stack story and incidentally fixed a latent D1 a11y addon
shape bug. D3 closes Phase 2 by adding the first vitest + RTL test
against a real React component. The test exists primarily to prove the
harness wiring works — `wrapResponsive`'s test in Phase 1 was a pure
function call with no JSX rendering, so this is the first time
@testing-library/react actually mounts something.

## Evaluator verdict

approved (first pass). Evaluator verified the single test, single
assertion shape, the vanilla RTL imports, the Stack-specific
assertion that requires React mounting, and clean
lint/build/test/test:agentic/build-storybook runs. The contract
mentioned jest-dom matchers as wired-via-Phase-1 but the assertion
uses plain `not.toBeNull()` — evaluator accepted as on-target given
the original ask is just "one vitest + RTL test against Stack" and
jest-dom availability isn't a requirement to invoke.

## Notes for the PR

D3 closes Phase 2 by adding the first vitest + RTL test against a
real React component. Phase 1's `wrapResponsive` test was a pure
function call with no JSX rendering, so this is the first time
`@testing-library/react` actually mounts something in this project.

Single test, single assertion: `<Stack as="section">` renders children
inside a `<section>`. Smallest meaningful proof that the React
testing harness end-to-end works for this codebase. Coverage
expansion is explicitly out of scope for the harness adoption phase
— per PLAN.md, "actual test coverage is explicitly deferred".
