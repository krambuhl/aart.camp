# Project config

## Verification
- `npm run lint`
- `npm run test`
- `npm run build`

## PR settings
- Base branch: main
- Reviewers: —
- Labels: project/draft-cli

## Worker bindings
Interactive loop. Single phase ships as one PR, human-paired throughout
(CLI verbs test-first, skill prose iterated against smoke tests, loop
wiring landed last).
