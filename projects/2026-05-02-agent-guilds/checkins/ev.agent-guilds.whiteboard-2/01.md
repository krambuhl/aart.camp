# Checkin 01 — ev.agent-guilds.whiteboard-2

**Created**: 2026-05-15 01:55
**Phase**: 3 — Whiteboard mechanism + engineers
**Unit**: D2 — Six `whiteboard-*` engineer agents + shared base

## Contract

- **Goal**: Author the six `whiteboard-*` engineer agents named in
  PLAN.md § Phase 3 (`whiteboard-react-architect`,
  `whiteboard-design-systems`, `whiteboard-performance`,
  `whiteboard-a11y`, `whiteboard-sketch-ideation`,
  `whiteboard-skeptic`), plus a shared `whiteboard-base.md`
  contract (parallel to `evaluator-base.md`) that establishes the
  read-only stance, section format, and per-engineer carve-out
  pattern. With D2 merged, the engineer roster is on disk and the
  bootstrapping-case skip in `/ev-loop-*`'s `### Whiteboard` step
  stops firing — phases starting from D3 forward will spawn the
  panel automatically.

  D2 is **agent-authoring only**: no engineer invocation, no
  whiteboard-step exercise, no `guild-whiteboard` skill edits.
  Per **L-004** the newly-authored engineers are session-
  boundary-gated; D3 (the design-phase end-to-end exercise that
  closes Phase 3) runs them in a fresh Claude Code session.

- **Acceptance criteria**:

  1. **Shared base** `.claude/agents/whiteboard-base.md` exists
     with frontmatter:
     - `name: whiteboard-base`
     - `role: whiteboard-base`
     - `description:` one paragraph naming the file as the
       shared base for the `whiteboard-*` family, disclaiming
       direct invocation (parallel to `evaluator-base.md`).
     - `tools: Read, Glob, Grep` (read-only — engineers do not
       write files; the `/guild-whiteboard` orchestrator does).
     - `model: inherit`
     - Body documents:
       - **Stance** (multi-perspective, constructive,
         opinion-with-reasoning, no agreement-for-its-own-sake).
       - **Input**: the brief from `/guild-spawn`, plus optional
         `per_agent_context` carrying prior whiteboard state for
         round 2+.
       - **Section format**: the engineer returns a coherent
         prose section that the orchestrator writes verbatim
         under `### From <engineer-name>` in the whiteboard
         file. No special framing; structured-but-conversational
         prose.
       - **Read-only constraint**: never write to the whiteboard
         file or any other file; never run mutating commands;
         the orchestrator does all writes.
       - **Cross-perspective courtesy**: name what's IN your
         domain and what's OUT (handled by sibling engineers).
         Don't pile on for the same finding another engineer
         would flag.

  2. **`whiteboard-react-architect`**
     (`.claude/agents/whiteboard-react-architect.md`) exists
     with frontmatter (`name`, `role: whiteboard-engineer`,
     `description`, `tools: Read, Glob, Grep`, `model:
     inherit`) and a body that:
     - Opens by instructing the agent to read
       `whiteboard-base.md` and apply its constraints.
     - Names the perspective: React API design — hook
       composition, server/client boundary, component
       composition (composition-over-configuration), prop API
       shape, when to lift state, when to push state to URL,
       when to use context.
     - Documents what this engineer leans toward: small
       composable primitives, predictable prop shapes,
       avoiding hidden behavior, server-component-first.
     - Lists boundary with sibling engineers (a11y, design-
       systems, performance, skeptic).
     - One short example of the perspective in action (≤ 10
       lines).

  3. **`whiteboard-design-systems`**
     (`.claude/agents/whiteboard-design-systems.md`) — same
     frontmatter shape; body covers:
     - Token usage (semantic over visual-literal).
     - Composition over configuration (high and low
       abstractions in parallel).
     - Naming as architecture — does the name match the
       semantic role, not the appearance?
     - The design-to-code handoff perspective (this is the
       user's day-job lens, per CLAUDE.md).
     - Boundary with tokens evaluator (D5 of Phase 2 owns
       literal-vs-token; this engineer owns the broader
       composability and naming question).

  4. **`whiteboard-performance`**
     (`.claude/agents/whiteboard-performance.md`) — body
     covers:
     - Bundle size (`'use client'` boundary placement, dynamic
       imports, dependency weight).
     - Hydration cost.
     - Server-component first; client features only when
       interactive.
     - Render cost (memoization, list virtualization,
       expensive operations in render).
     - Boundary with nextjs evaluator (D2 of Phase 2 owns
       framework-correctness; this engineer owns the
       performance-cost-of-choices angle).

  5. **`whiteboard-a11y`**
     (`.claude/agents/whiteboard-a11y.md`) — body covers:
     - Semantic HTML first (use `<button>` not `<div
       onClick>`).
     - Focus management (where does focus go on this
       interaction?).
     - Screen-reader experience (what does the rendered tree
       announce?).
     - Keyboard parity (everything mouseable is also
       keyboardable).
     - Boundary with a11y evaluator (D1 of Phase 2 owns
       catalog-hit detection on shipped code; this engineer
       owns the design-phase advocacy for inclusive design
       upstream of code).

  6. **`whiteboard-sketch-ideation`**
     (`.claude/agents/whiteboard-sketch-ideation.md`) — body
     covers:
     - Generative-art idioms specific to aart.camp (p5.js,
       `<Sketch>` wrapper, sketch numbering convention).
     - Creative direction (when is a sketch about exploring a
       single visual idea vs. composing multiple ideas?).
     - Aesthetic decisions (color, motion, density,
       composition).
     - The unique tone of this site — playful, intentional,
       a generative-art portfolio rather than a product.
     - Carve-out: this engineer is the right voice only when
       the design phase touches sketches/ or new sketch
       infrastructure. For substrate or shared-component
       design phases, this engineer notes it's not in scope
       (per the base's read-only-but-honest stance).

  7. **`whiteboard-skeptic`**
     (`.claude/agents/whiteboard-skeptic.md`) — body covers:
     - Devil's-advocate stance: what could go wrong with
       this design?
     - Edge cases the other engineers might gloss over.
     - Hidden complexity (when does a "simple" choice
       compound?).
     - When "good enough for now" is fine vs. when it
       compounds.
     - This engineer's job is to pressure-test, not to gate.
     - Boundary: the skeptic engineer is constructive
       (find-the-real-risk), not contrarian
       (find-a-reason-to-doubt). The whiteboard isn't a
       blocking step.

  8. **All seven agent files typecheck under Biome / build**.
     They're `.md` files outside the build graph, so this is
     a trivial gate — but `npm run lint` and `npm run build`
     still pass post-D2.

  9. **L-004 acknowledgment in the checkin Notes for the
     PR**: D2 ships the engineers, but they aren't callable
     in this session (the agent registry is loaded once per
     Claude Code process start). D3 invokes them in a fresh
     session — this is the explicit session-boundary split
     the panel-8 D8 smoke test established.

  10. **Verification**:
      - `npm run lint` (Biome) → exit 0.
      - `npm run build` (Next) → exit 0.
      - `npm test` (vitest) → exit 0; test count unchanged
        (no script changes).
      - `git status --short` (fresh per **L-005**) shows
        only the expected new/modified paths.

  11. **D2 own panel** auto-derives to
      `evaluator-contract-fit` only — all artifacts are
      `.claude/agents/*.md`, which per PANEL-COMPOSITION.md
      classify as contract-fit-only ("substrate authoring;
      domain evaluators don't apply to agent definitions").
      Single-evaluator panel. No L-004 override needed.

- **Rules applied** (re-verified at close):
  - `npm run lint` clean.
  - `npm run build` clean.
  - `npm test` passes; no new test cases (engineers are
    `.md` files; no script behavior changes).
  - **L-001** honored: no `tsx` invocation introduced.
  - **L-002** honored: surfaced engineer-count and
    base-file inclusion decisions to the user before
    writing.
  - **L-004** explicitly noted in Notes for PR — the
    engineers are session-cached after D2 lands; D3 runs
    them in a fresh session.
  - **L-005** honored: fresh `git status --short` before
    evaluator.
  - **L-009** honored: dense packet for the D2 own-eval
    panel.
  - **L-013** honored: every claim about engineer-file
    structure cites the actual file content.

- **Disqualifiers**:
  - No engineer-invocation smoke test. D3 territory.
  - No design-phase exercise. D3 territory.
  - No edits to `/guild-whiteboard` SKILL, `whiteboard.ts`,
    `whiteboard.test.ts`. D1 territory; D2 only adds
    consumers (the agent files).
  - No edits to `/ev-loop-*` SKILLs. D1 wired the integration
    already; D2 only adds engineers.
  - No edits to `evaluator-*.md`, `evaluator-base.md`,
    `PANEL-COMPOSITION.md`, `derive-panel.ts`,
    `parse-and-aggregate.ts`. Phase 2 territory.
  - No edits to `/guild-spawn`, `/guild-validate`,
    `/trout-*`, `/griot-*` skills or scripts.
  - No edits to `projects/CONVENTIONS.md` (the PLAN.md
    phase-config note from D1 still covers the whiteboard
    field; no engineer-related convention additions
    needed).
  - No edits to `.claude/settings.json` — engineers are
    agent files, not scripts; no new permissions needed.
  - No edits to product code (`components/`, `sketches/`,
    `app/`, `tokens/`, `styles/`).
  - No `tsx` invocation anywhere.

- **Inputs**:
  - `.claude/agents/evaluator-base.md` — structural reference
    for the new `whiteboard-base.md` (frontmatter shape,
    body sections, "not directly callable" disclaimer).
  - `.claude/agents/evaluator-*.md` — structural reference
    for the per-engineer body shape (stance, domain rubric,
    carve-outs, boundary with siblings).
  - `.claude/skills/guild-whiteboard/SKILL.md` — the
    orchestrator that will spawn these engineers; reference
    for "engineers are read-only" and section format.
  - `.claude/CLAUDE.md` — context for the user's stack
    (React, TypeScript, CSS Modules, p5.js, design-system
    philosophy) that informs the React-architect,
    design-systems, performance, a11y, and sketch-ideation
    engineers.
  - `projects/2026-05-02-agent-guilds/PLAN.md` § Phase 3 —
    the engineer roster.

## Execution

1. **Branch cut + prelude commit** (`6e1b96f`). Stashed reconcile
   diff from `autoload --reconcile` (recorded `#55 (open) →
   merged`), checked out main, pulled `origin/main` (`9abe161 →
   a7ef282` — squash-merge of #55), cut
   `ev.agent-guilds.whiteboard-2` from updated main, popped the
   stash, autosaved the branch-cut note, committed the manifest
   mutations.

2. **Surfaced the engineer-count + base-file inclusion fork** to
   the user (per **L-002**). Options: execute as written (6 + base),
   skip the base, or split into two PRs. User picked execute as
   written.

3. **Authored `whiteboard-base.md`** following the
   `evaluator-base.md` structural shape: frontmatter (`name`,
   `role: whiteboard-base`, `description`, `tools: Read, Glob,
   Grep`, `model: inherit`); body covers Stance,
   Input (brief + optional prior whiteboard state),
   Output (prose section, no verdict line), Read-only,
   Cross-perspective courtesy, "What you never do." ~110 lines.

4. **Authored the six engineer agents**, each ~120-180 lines.
   Each opens by instructing the agent to read
   `whiteboard-base.md` and apply its constraints. Each
   establishes its perspective, lists what it leans toward, names
   its boundary with every sibling engineer, includes a carve-out
   when relevant (sketch-ideation defers entirely on
   substrate/shared-component briefs), and closes with one
   concrete example perspective.
   - `whiteboard-react-architect`: hook composition, server/client
     boundary, prop API shape, state location.
   - `whiteboard-design-systems`: semantic over literal,
     composition over configuration, naming as architecture,
     design-to-code handoff, carve-out vs. `evaluator-tokens`.
   - `whiteboard-performance`: bundle size, `'use client'`
     boundary, hydration cost, render cost, data flow, carve-out
     vs. `evaluator-nextjs`.
   - `whiteboard-a11y`: semantic HTML, focus management,
     screen-reader experience, keyboard parity, color as
     semantic, motion + reduced-motion, carve-out vs.
     `evaluator-a11y`.
   - `whiteboard-sketch-ideation`: one-idea-per-sketch,
     generative not illustrative, p5.js idioms, numbered-
     sketch convention, portfolio framing, **explicit
     carve-out when the design phase is substrate or shared-
     component work** (defer entirely).
   - `whiteboard-skeptic`: pressure-test consensus, concrete
     concerns with remedies, constructive not contrarian,
     match-stakes calibration, multi-round dynamic (round 2+ is
     where the skeptic earns the most keep).

5. **Pre-verification**. `npm run lint` and `npm run build` both
   green — agent files are `.md` outside the build graph. `npm
   test` not re-run (no code changes); test count unchanged at
   18 files / 322 tests.

6. **Antagonist panel**. Derive-panel.ts on the seven new agent
   files + checkin file returns `evaluator-contract-fit` only
   (all artifacts are `.claude/agents/*.md`, which classify as
   contract-fit-only per PANEL-COMPOSITION.md's substrate-
   authoring carve-out). Single-evaluator panel; no L-004
   override needed (contract-fit existed at session start).

## Scope

- **Created**:
  - `.claude/agents/whiteboard-base.md` — shared base contract.
  - `.claude/agents/whiteboard-react-architect.md`
  - `.claude/agents/whiteboard-design-systems.md`
  - `.claude/agents/whiteboard-performance.md`
  - `.claude/agents/whiteboard-a11y.md`
  - `.claude/agents/whiteboard-sketch-ideation.md`
  - `.claude/agents/whiteboard-skeptic.md`
  - `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.whiteboard-2/01.md`
    (this checkin).

- **Modified** (in prelude commit `6e1b96f`):
  - `projects/2026-05-02-agent-guilds/MANIFEST.md` — phase 2 row
    flipped `#55 (open) → merged`, phase-3 current-branch
    pointer to whiteboard-2, three event rows.

- **Not touched** (per disqualifiers):
  - `/guild-whiteboard` SKILL, `whiteboard.ts`,
    `whiteboard.test.ts` — D1 territory.
  - `/ev-loop-*` SKILLs — D1 wired the integration.
  - `evaluator-*.md`, `evaluator-base.md`,
    `PANEL-COMPOSITION.md`, `derive-panel.ts`,
    `parse-and-aggregate.ts` — Phase 2 territory.
  - `/guild-spawn`, `/guild-validate`, `/trout-*`, `/griot-*`.
  - `projects/CONVENTIONS.md`, `.claude/settings.json`,
    `package.json`, `biome.json`, `tsconfig.json`.
  - Product code.

## Pre-computed verification (authoritative)

- `npm run lint` (Biome) → exit 0; "Checked 129 files in 33ms.
  No fixes applied. Found 2 warnings." Same 2 pre-existing
  dereferenced-symlink warnings on unused skill stubs,
  unchanged.
- `npm run build` (Next) → exit 0; static + SSG prerendered.
  Agent files are `.md` and outside the build graph; no
  typecheck impact.
- `npm test` (vitest) → not re-run; no code changes. The 18
  files / 322 tests baseline from D1 stands.
- `git status --short` (fresh per **L-005**) → 7 untracked
  agent files + 1 untracked checkin dir. Tree otherwise clean;
  MANIFEST.md committed in prelude `6e1b96f`.
- `derive-panel.ts` on D2's artifact file list →
  `evaluator-contract-fit` only (validated by dogfood per
  **L-013**). Single-evaluator panel.

## Evaluator verdict

**Panel**: `evaluator-contract-fit` only. All artifacts are
`.claude/agents/*.md`, which classify as contract-fit-only via
PANEL-COMPOSITION.md's substrate-authoring carve-out. No L-004
override required.

**Run 1 of 1**: `approved`. contract-fit confirmed:
whiteboard-base.md frontmatter + body sections complete;
each engineer file follows the same template structure
(read-the-base opener, perspective, leans-toward, sibling
boundaries, carve-out where relevant, example); sketch-
ideation's explicit "When you carve yourself OUT" section
load-bearingly satisfies AC 6. Disqualifier surface clean —
only `.claude/agents/whiteboard-*.md` files + the checkin
dir + the prelude MANIFEST mutation. No edits to forbidden
paths.

**No disqualifier fired.** No engineer invocation, no
design-phase exercise.

## Notes for the PR

- **L-004 strengthened acknowledgment**: D2 introduces six
  new `whiteboard-*` engineer agents + `whiteboard-base.md`.
  Per the **L-004** strengthening from panel-8 D8 of Phase 2,
  newly-authored agent files are NOT callable in the
  current Claude Code process — the agent registry loads
  once per process start, and `/clear` is NOT a session
  boundary. D3 (the real design-phase exercise that closes
  Phase 3) MUST run in a fresh Claude Code process, started
  AFTER this PR merges and the engineer files are on disk.
  D3's contract will require the session-restart explicitly.

- **Coarse decomposition honored**. Per the user's earlier
  "3 deliverables, coarse" Phase 3 decomposition choice (D1
  primitive + ev-loop integration; D2 all six engineers +
  base; D3 design-phase close), D2 bundles all seven agent
  files in one PR. The proven Phase 2 pattern was finer
  (one PR per evaluator), but each whiteboard engineer is
  shorter and more conversational than the evaluator-*
  agents, and the shared template structure makes them
  cohesive as a batch. Single-PR review burden is real but
  manageable.

- **Shared base file authored** (`whiteboard-base.md`),
  parallel to `evaluator-base.md`. Each engineer's body
  opens by instructing the agent to read the base and apply
  its constraints. The base owns: multi-perspective stance,
  read-only constraint, prose-section output format, cross-
  perspective courtesy (lane-deferral rules), and the
  "never do" list. Each engineer adds: its perspective,
  what it leans toward, named boundaries with every sibling,
  carve-outs where domain doesn't apply, one concrete
  example.

- **Sketch-ideation engineer has an explicit "When you
  carve yourself OUT" section**. This is the engineer most
  prone to over-firing on irrelevant briefs (a substrate
  design phase shouldn't get sketch-aesthetic input). The
  carve-out section names substrate work, shared-
  infrastructure components, design tokens, and non-sketch
  routes as defer-entirely cases. Documented per AC 6.

- **No `disable-model-invocation` on agent files**. Agent
  frontmatter doesn't take this field (it's a skill-only
  concept). The substrate-composition concern from PR #55
  (where `disable-model-invocation: true` blocked the Skill
  tool from invoking guild-* primitives) doesn't apply to
  agent files at all — agents are spawned via the Agent
  tool, which has different gating semantics
  (`user-invocable: false` is the gate on agents).

- **L-001 honored**: no `tsx` invocation. Agent files are
  `.md`, no scripts authored in D2.
- **L-002 honored**: surfaced the engineer-count + base-
  file inclusion fork to the user before authoring; user
  picked "execute as written."
- **L-005 honored**: fresh `git status --short` immediately
  before the evaluator panel; recorded verbatim in
  § Pre-computed verification.
- **L-009 honored**: dense packet for D2's own-eval panel
  with pre-computed lint/build/test/derive-panel outputs.
- **L-013 honored**: every claim about engineer-file
  structure cites the actual artifact content (the
  evaluator-contract-fit spot-checks confirmed both the
  base file and the sketch-ideation carve-out at exact
  file path references).

- **Phase 3 sequencing**: D1 (primitive + integration)
  merged in #55. D2 (this PR) ships the engineer roster.
  D3 closes Phase 3 by running the design-phase exercise
  end-to-end in a fresh session — the first real exercise
  of the whole `/guild-whiteboard` + engineer-panel
  pipeline, parallel to the panel-8 D8 multi-evaluator
  smoke test for Phase 2.
