# Checkin 05 — ev.agent-guilds.substrate-foundations

**Created**: 2026-05-04 10:07
**Phase**: 1 — Substrate foundations
**Unit**: 4b — Runtime-verify substrate path; retire evaluator.md

## Contract

- **Goal**: Close the deferred half of unit 4. Spawn `subagent_type: evaluator-contract-fit` against a small synthetic packet to confirm the new agent is callable in this fresh session (registry refreshed). If the verdict is parseable in the locked output shape from `evaluator-base.md`, delete `.claude/agents/evaluator.md` and verify no stale `subagent_type: evaluator` references remain anywhere in the substrate. This closes Phase 1.

- **Acceptance criteria**:
  - Spawning `subagent_type: evaluator-contract-fit` via the `Agent` tool succeeds (the agent registry was refreshed by the session restart). The agent returns a response containing a parseable `VERDICT:` line — `approved` or `flagged`. Whether the verdict is approved or flagged is irrelevant; the test is shape conformance.
  - `.claude/agents/evaluator.md` is deleted from the working tree, **only after** the spawn above succeeds.
  - No remaining `subagent_type: evaluator\b` (the bare evaluator name, distinct from `evaluator-contract-fit` or `evaluator-base`) references in `.claude/skills/` or `.claude/agents/`. Verified via grep.
  - `npm run lint` (Biome) clean.
  - `git status` after the deletion shows only the expected changes: `D .claude/agents/evaluator.md` and the new checkin 05.

- **Rules applied**:
  - Read-only verification first; mutating action (deletion) only after verification passes.
  - `~/.claude/CLAUDE.md` and `aart.camp/.claude/CLAUDE.md` conventions: no emojis, no speculative abstractions.

- **Disqualifiers**:
  - Spawn of `evaluator-contract-fit` returns "Agent type not found" (would mean the registry didn't refresh as expected). If this fires, do NOT delete evaluator.md — surface to user and pause.
  - `evaluator.md` is deleted before the spawn verification succeeds. (Strict ordering: verify, then delete.)
  - Stale `subagent_type: evaluator\b` references remain after the deletion. Loops or other agent files would point at a missing agent.
  - Lint regressions introduced by this unit (it shouldn't introduce any — only deleting one file).

- **Inputs**:
  - `.claude/agents/evaluator.md` (target of deletion)
  - `.claude/agents/evaluator-base.md` (verdict shape reference)
  - `.claude/agents/evaluator-contract-fit.md` (the agent to spawn for verification)
  - `.claude/skills/ev-loop-confidence/SKILL.md`, `.claude/skills/ev-loop-interactive/SKILL.md` (already migrated in unit 4 — should reference evaluator-contract-fit, not evaluator)
  - `~/.claude/projects/-home-krambuhl-work-aart-camp/memory/project_agent_registry_session_cache.md` (the memory note documenting why this unit had to be deferred)
  - `projects/2026-05-02-agent-guilds/sessions/2026-05-03-a.md` (last session handoff; "Open threads" §1 is exactly this unit)
  - `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.substrate-foundations/04.md` (the deferring contract)

## Scope

Files deleted:
- `.claude/agents/evaluator.md` (the bridge file preserved through Phase 1)

Files created:
- `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.substrate-foundations/05.md` (this checkin)

No source code touched. No build-affecting changes.

## Execution

**Step 1 — Runtime verification.** Spawned `subagent_type: evaluator-contract-fit` via the `Agent` tool with a synthetic packet (Goal: "verify spawn path is callable", no real work). The agent registry refreshed at session start as expected; the agent loaded, read `.claude/agents/evaluator-base.md`, and returned a parseable verdict in the locked shape:

```
VERDICT: approved

Summary: Synthetic spawn smoke test — the agent loaded, read the inherited base contract, and is emitting a verdict in the locked shape.

Checks:
- Read evaluator-base.md: met
- Emit VERDICT line in locked shape: met
- Disqualifiers: none fired
- Rules: none specified, nothing to run
- Ask alignment: on target — substrate path is callable; bridge deletion is unblocked from this evaluator's side
```

The verification confirms the full substrate chain: ev-loop → `/guild-validate` (Skill) → `/guild-spawn` (Skill) → `Agent` (`subagent_type: evaluator-contract-fit`) → `evaluator-base.md` reference → verdict emission. End-to-end.

**Step 2 — Delete the bridge file.** `rm .claude/agents/evaluator.md`. Confirmed via `git status`: shows `deleted: .claude/agents/evaluator.md`.

**Step 3 — Stale reference scan.** `grep -rn "subagent_type: evaluator\b" .claude/` returned no matches. The migrated ev-loop skills already reference `evaluator-contract-fit`; no other skill or agent file references the bare `evaluator` subagent_type. Clean.

**Step 4 — Lint.** `npm run lint` passes with the same 2 baseline broken-symlink warnings on unrelated files.

Phase 1 is **structurally complete**. The substrate (`guild-spawn` + `guild-validate`) is wired, the loops compose it, the antagonist family pattern (`evaluator-base.md` + `evaluator-contract-fit.md`) is established, and the legacy `evaluator.md` is retired.

## Changes

```
D   .claude/agents/evaluator.md
A   projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.substrate-foundations/05.md
M   projects/2026-05-02-agent-guilds/MANIFEST.md  (will be updated post-evaluation)
```

The `.claude/settings.local.json` modifications from prior sessions remain unstaged — still local-config noise, not part of any unit's conceptual change.

## Notes for PR

- Phase 1 closes here. The branch has 5 commits (4 substrate units + this cleanup) ready for a single PR.
- Reviewers should verify: (1) `guild-spawn` + `guild-validate` skills are well-scoped substrate primitives, (2) ev-loop-confidence/interactive cleanly compose `/guild-validate` instead of spawning evaluator directly, (3) `evaluator-base.md` is domain-agnostic and `evaluator-contract-fit.md` only adds the contract-walking rubric, (4) no `subagent_type: evaluator\b` references remain.
- The Phase 1 → Phase 2 handoff: `evaluator-base.md`'s flag taxonomy is the starter set. Phase 2 evaluators (a11y, nextjs, react-api, tokens, naming) will add domain-specific codes per their antipattern catalogs. The `BLOCKING:` / `ADVISORY:` prefix convention referenced in `guild-validate`'s v1 compatibility rule is what those evaluators will start emitting natively.
- The `flagged-conflict` verdict from `guild-validate` cannot be produced in v1 single-evaluator panels but is in the API. Phase 2 multi-evaluator panels will populate it once conflict-detection logic is added to `guild-validate` step 4.

