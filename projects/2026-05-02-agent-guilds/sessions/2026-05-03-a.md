# Session 2026-05-03-a

**Phases touched**: 1
**Checkins written**: 01, 02, 03, 04
**PR activity**: none (deferred to phase close)

## What happened

Phase 1 substrate foundations substantially landed. Four commits on `ev.agent-guilds.substrate-foundations`: extracted `evaluator-base.md` and renamed the existing evaluator to `evaluator-contract-fit.md` (unit 1, kept evaluator.md as bridge), authored `guild-spawn` (unit 2, iteration 2 after the evaluator caught me dropping `per_agent_context` on YAGNI grounds), authored `guild-validate` (unit 3, first pass; output shape is locked to the design plan's full spec even though v1 leaves several fields empty), and migrated both ev-loop skills to invoke `/guild-validate` via the `Skill` tool (unit 4, iteration 2 against an amended contract).

The amendment was forced by a real architectural finding: Claude Code's agent registry is loaded at session start, so `evaluator-contract-fit` (added in unit 1 of this session) wasn't callable as a `subagent_type` for the runtime-verification step in unit 4. Split unit 4 into 4 (loops migrated) + 4b (runtime verify + retire `evaluator.md`, first action next session). evaluator.md is preserved as the session bridge.

Pattern that repeats across this session: when the artifact deviates from the contract, don't let the artifact silently rewrite the contract via Execution-section rationale. Either restore the missing piece (unit 2: per_agent_context) or amend the contract first and re-evaluate (unit 4 iteration 2). The evaluator caught both.

## Open threads

- **Unit 4b (next session, first action)**: spawn `evaluator-contract-fit` against a synthetic packet to runtime-verify the substrate path is callable. If the verdict is parseable, delete `.claude/agents/evaluator.md` in a small commit and grep to confirm no remaining references. Then close Phase 1 and open the PR via `/trout-pull-request`.
- **PR description**: when 4b lands and the PR opens, the description should call out that the migration is **structurally** complete in 4 commits but functionally requires a session restart to exercise (and that 4b is the proof). Reviewers should not be alarmed by `evaluator.md` still existing in the diff until 4b commits.
- **Phase 2 design choices to revisit**: the verdict-shape "lock the full shape now, populate empty in v1" decision (unit 3) is forward-compatible for Phase 2 evaluators with antipattern catalogs. When Phase 2 starts, the evaluator base contract may need updating to require explicit `BLOCKING:` / `ADVISORY:` prefixes; currently the v1 compatibility rule treats unprefixed reasons as blocking.
- **Memory note**: agent-registry session cache lesson is now in project memory at `~/.claude/projects/-home-krambuhl-work-aart-camp/memory/project_agent_registry_session_cache.md`. This affects every future phase that introduces new agent files (Phase 2: 5 evaluators, Phase 3: 6 whiteboard engineers, Phase 4: 4 domain pairs).

## Learnings captured

- `learnings/session-notes/2026-05-04T03-07-19-substrate-api-stability/` — from `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.substrate-foundations/02.md` (substrate API shape stability beats in-flight YAGNI; amend the contract first if the spec genuinely should narrow).
- `learnings/session-notes/2026-05-04T03-08-00-agent-registry-session-cache/` — from `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.substrate-foundations/04.md` (Claude Code's agent registry is session-cached; don't bundle agent-creation with agent-use in one unit).

## Notes

- `.claude/settings.local.json` has been incidentally modified throughout this session by the harness (auto-added Skill permissions). Left unstaged; not part of any unit's conceptual change. Will need to be staged in a separate commit or just allowed to ride along with whatever next-session commit makes sense.
- The branch is `ev.agent-guilds.substrate-foundations` (dot-segmented per the user's branch convention saved to memory). Four commits: `a8f1cfd` (unit 1), `226745b` (unit 2), `321060a` (unit 3), `4374a57` (unit 4). All ahead of `main`.
- TaskList has unit 4b as `pending` (#5). Drop into it next session.
