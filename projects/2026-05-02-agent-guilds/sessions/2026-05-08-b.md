# Session 2026-05-08-b

**Phases touched**: 1.5
**Checkins written**: 08 (on ev.agent-guilds.phase-1-5-substrate-cleanup-3)
**PR activity**: #16 merged at session start, #18 opened (cleanup-3, single checkin)

## What happened

Picked up after PR #16 merged earlier today. The router caught the post-merge state-propagation gap that 2026-05-08-a's handoff had flagged: MANIFEST still recorded #16 as open even though it was merged. Reconciled by running `autosave --event=pr-merged`, cutting `ev.agent-guilds.phase-1-5-substrate-cleanup-3` from updated `main`, and updating the phase row to point at the new branch. The merge from main brought in PRs #14 (storybook) and #15 (playwright) which had moved the project's test runner to vitest.

Then completed deliverable 6's second half — the `/trout-pull-request` SKILL.md rewrite that consumes the `pr-plumbing.ts` verbs landed in checkin 07 of #16. Sections 1-3 collapse into one Inspect step, sections 5-6 collapse into one Submit step, section 4 (LLM-shaped title/body authoring) preserved as the heart of the skill. Tool surface shrunk from `Read, Write, Edit, Bash(git:*), Skill, mcp__github__*` (4 MCP tools + git) down to `Read, Write, Edit, Skill, Bash(node .claude/scripts/trout/*)`. Added Invariant 9 codifying the seam: all git/gh/autosave actions flow through script verbs.

Mid-checkin, discovered 4 of 5 substrate-script test files still on `node:test` while `npm run test` post-merge invokes vitest — only `autosave.test.ts` had been migrated as part of #16's merge resolution. Surfaced to user, who chose to migrate the 4 files in this checkin (option 1) rather than defer. Applied a 2-line shim per file: prepend `// @vitest-environment node`, replace `import { test } from 'node:test'` with `import { test } from 'vitest'`. `node:assert/strict` stays as-is (vitest tolerates). All 121 tests now green.

The dogfood checkpoint (using the rewritten skill to author its own PR) caught a real prose-vs-runtime mismatch: my SKILL.md said `pr-plumbing.ts submit` substitutes `<N>` in `--phase-update` from the gh response — empirically false. The phase row landed with literal `#<N> (open)` text. Fixed the prose to describe the actual two-step pattern (submit without `--phase-update`, then a follow-up autosave). Manually corrected the phase row to `#18 (open)`. Lesson: evaluator-by-reading approves contract structure; dogfooding catches behavior. The dogfood step is more load-bearing than I gave it credit for.

## Open threads

- **Substrate refinement backlog has grown to 5 items**, all candidates for a future maintenance checkin once Phase 1.5's deliverable backlog clears (D7-D12 still open):
  1. `autoload.ts` should verify PR open/merged via `gh` rather than trusting MANIFEST alone — would have closed the post-merge state-propagation gap that required manual user intervention twice now (sessions 2026-05-08-a and -b).
  2. `pr-plumbing.ts` `commit` verb's untracked-files allowlist is too permissive for code outside `projects/`. Tighten to a curated set of code dirs.
  3. `pr-plumbing.ts` `submit` should sanity-check that `--phase-update`'s `pr=#N` matches the gh-returned PR number (would have caught the `#13` typo in PR #16, and the literal `<N>` in PR #18).
  4. `autosave --phase-update` doesn't propagate to MANIFEST `**Current branch**` header field — requires manual Edit. NEW from session -b.
  5. `pr-plumbing.ts submit` should accept `<N>` placeholder and substitute the gh-returned number, OR the SKILL.md two-step pattern should be promoted to a single verb. NEW from session -b's dogfood.
- **Test-runner architectural question deferred**: per-file `// @vitest-environment node` directive vs. project-level vitest config rule for `.claude/scripts/**`. Either works; per-file matches the autosave precedent. Worth a deliberate decision before the next test file lands.
- **Stray session 2026-05-08-a.md** that didn't ship with PR #16 was carried into PR #18 in commit `1e931ac` — handled, but the underlying pattern (session-save writes file but doesn't commit it) is part of refinement #1 / #4 territory.
- **PR #18 includes one corrected commit** (`1e931ac`) for dogfood-discovered prose bug + the stray session file. Reviewer should not be confused by the multi-commit PR — the substantive work is in `0b2a812` (checkin 08), the rest is housekeeping.
- **Next session's first work** per PLAN: D7 trout-archive relocate split. Same shape as D3/D4 migrations — author `.claude/scripts/trout/archive-relocate.ts` + sibling tests, update `trout-archive` SKILL.md to shell out for the relocate step, keep the interview/retro/synthesis prose LLM-shaped.

## Learnings captured

- `learnings/session-notes/2026-05-08T15-57-28-line-target-llm-content/` — from `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.phase-1-5-substrate-cleanup-3/08.md`. Lesson: line-count targets in LLM/CRUD-split contracts can be miscalibrated when most of the skill body is genuine LLM-shaped content (templates, prompts, structural shapes). Don't set aggressive line targets if the skill body is mostly content the script can't absorb.
- `learnings/session-notes/2026-05-08T16-02-34-dogfood-catches-prose-bugs/` — from same checkin. Lesson: evaluator-by-reading approves contract structure but cannot catch prose-vs-runtime mismatches. Dogfooding the rewritten artifact at the loop's checkpoint step is more load-bearing than the contract treats it; treat dogfood as a first-class verification, not a "nice to confirm."

## Notes

- Phase 1.5 progress: 6.5/12 deliverables done. D7-D12 remaining (trout-archive, trout-save-session finalize, trout-pr-respond, trout-plan, griot-use → script + ev-run inline, e2e verification on phase-1-5-test).
- The 6 deliverables left ought to fit comfortably in 1-2 more cleanup PRs depending on natural-pause spacing. Cleanup-2 hit the natural-pause threshold at 7 checkins; cleanup-3 closing at single-checkin makes future PR-shape decisions cleaner.
- Substrate state at session end: branch `ev.agent-guilds.phase-1-5-substrate-cleanup-3` clean, PR #18 open, 121/121 tests green, 5 substrate refinements backlogged, 2 fresh learning captures pending `/griot-compact`.
