# Session 2026-05-15-end

**Phases touched**: 2, 3
**Checkins written**: panel-8/01 (Phase 2 close), whiteboard/01 + 02 + response-02 (D1 + response), whiteboard-2/01 (D2), whiteboard-3/01 (D3 contract draft only)
**PR activity**: #52 opened+merged (Phase 2 D8 close), #55 opened+merged (D1 + response), #58 opened+merged (D2), #59-pending (D3 not yet opened)

## What happened

Closed Phase 2 with the D8 multi-evaluator smoke test (PR #52). The smoke
test surfaced two substrate findings worth keeping: `parse-and-aggregate.ts`
chokes on `VERDICT: approved — <em-dash trailing note>` (the parser
expects a clean VERDICT line; em-dash continuation produces a spurious
parse-failure), and `/clear` is **not** a session boundary for the agent
registry — that requires a Claude Code process restart. Both got logged as
learning candidates rather than fixed in scope.

Opened Phase 3 with the user's "3 deliverables, coarse" decomposition:
D1 (`/guild-whiteboard` primitive + helper script + 26 vitest cases +
ev-loop integration in both loops) shipped in #55. Review feedback on #55
flipped the whiteboard step from opt-in to always-on-with-defaults
(per-engineer defaults via glob, phase-name topic, rounds=1, bootstrapping
skip when no engineers registered) and ran `/review-skill` on the new
guild-whiteboard skill body — the audit's Critical
`disable-model-invocation: true` finding was applied and **immediately
reverted** when the next `/guild-validate` invocation in the same session
failed: that gate blocks Skill-tool composition, which is exactly the
substrate pattern the `guild-*` family is built on. That's the most
elevation-worthy correction from this session — the audit tool's Critical
is correct for user-facing skills but wrong for substrate composition
primitives.

D2 shipped the six `whiteboard-*` engineer agents + a shared
`whiteboard-base.md` in #58. Each engineer follows the same template
(read-base opener / perspective / leans-toward / sibling boundaries /
carve-out / example); the sketch-ideation engineer has a load-bearing
"When you carve yourself OUT" section for non-sketch design phases.

D3 (the design-phase end-to-end exercise that closes Phase 3) requires a
fresh Claude Code process — the engineers in #58 aren't in this session's
registry. The D3 contract is drafted and committed on
`ev.agent-guilds.whiteboard-3` with topic *"Should aart.camp adopt a
draft/published distinction for sketches?"* — chosen because it engages
multiple engineers' genuine perspectives (sketch-ideation strongly
against, design-systems mixed, skeptic worried about feature creep) and
should produce real contradictions for round 2 to resolve. User
confirmed the restart-now-resume-D3 path.

## Open threads

- **D3 ready to execute in fresh session**. After restart: run `/ev-run
  agent-guilds` to orient onto `ev.agent-guilds.whiteboard-3`, then
  invoke `/guild-whiteboard` for round 1 with all six engineers against
  the drafted design topic, then round 2 with the same params (auto-
  detects round 2 from file state). Contract is at
  `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.whiteboard-3/01.md`;
  12 ACs covering append correctness + contradiction handling + a
  results doc + the L-004 documentation.
- **parse-and-aggregate.ts VERDICT-line leniency**. Substrate parser
  finding from panel-8 D8 is unfixed. Worth a small follow-up unit
  after Phase 3 closes (Phase 1.5-style carryover, or rolled into
  Phase 4's first PR). The packet-side workaround (instruct evaluators
  to put trailing notes on a separate line) is in active use and works.
- **L-004 strengthening worth capturing as a learning**. Two data
  points now: panel-8 D8 (Phase 2 multi-evaluator smoke test) and D3
  pre-execution. Pattern is clear — `/clear` clears message history
  but not the agent registry; only Claude Code process restart does.
  The current L-004 text says "session restart" without distinguishing
  the two; future loops should treat process-restart as the explicit
  requirement when newly-authored agents need to be invoked.
- **disable-model-invocation on composition primitives** worth
  elevating to the griot pipeline (`/griot-compact` next time). The
  apply-then-revert in #55's response is documented in checkin 02's
  Notes for the PR.
- **Phase 4 starts after #59 (D3's PR) merges**. PLAN.md § Phase 4
  describes `generator-css-codemod` + `evaluator-css-architecture`
  (active pair) plus three stub pairs. First Phase 4 phase-start will
  automatically fire the whiteboard step with the six engineers
  available — the always-on default from D1 finally activates for
  real.

## Notes

- Heavy session: 5 substantive PRs (#52, #55, #58, plus the in-flight
  D3) across Phase 2 close + Phase 3 D1, D1-response, D2, D3 contract.
  Substrate convention held throughout — every PR has a prelude
  commit, every unit had a contract surfaced via AskUserQuestion
  before execution, every unit ran its own antagonist panel.
- The `disable-model-invocation` revert was the most surprising
  finding of the session — the `/review-skill` audit tool is sound,
  but its recommendations need a "substrate composition primitive"
  carve-out. Captured in checkin 02's Notes for the PR + the
  smoke-results doc for the griot pipeline to pick up next compact.
