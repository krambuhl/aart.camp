# Session 2026-05-15-end

**Phases touched**: 3, 4
**Checkins written**: whiteboard-3/01 (Phase 3 close), domain-pairs/01 (Phase 4 D1), domain-pairs-2/01 (Phase 4 D2), domain-pairs-3/01 (Phase 4 D3 contract only)
**PR activity**: #64 opened+merged (Phase 3 close), #71 opened+merged (Phase 4 D1), #73 opened+merged (Phase 4 D2), #75-pending (D3 not yet opened)

## What happened

Resumed from the 2026-05-15-a handoff in a fresh process to execute
Phase 3 D3 (the design-phase end-to-end exercise that closes Phase 3).
The 2-round whiteboard on *"Should aart.camp adopt a draft/published
distinction for sketches?"* worked exactly as the substrate was
designed to — six engineers in parallel, real disagreements
surfaced in round 1, observable position shifts in round 2 (the
skeptic synthesized a position the panel hadn't named: gitignored
`_drafts/` + dev-only route + zero registry changes). Phase 3
closed with #64 merging.

Then opened Phase 4 (Domain pairs) with the always-on whiteboard
step firing automatically for the first time. The round-1 panel
produced **two material reframes of PLAN.md's Phase 4 prescription**
before any code landed:

- **Skeptic flipped the specialist-sequencing directionality**.
  PLAN.md said "specialist solo first, panel only on approval."
  Skeptic argued that has it backward — generator + specialist are
  aligned-by-construction, so approval-skips-panel is "the author
  marking their own homework." User adopted the flip: panel runs
  always with specialist at elevated precedence, fail-fast only on
  rejection. **Net effect: no ev-loop control-flow change** — the
  existing PANEL-COMPOSITION precedence mechanism carries the
  pattern.
- **Three stubs dropped** in favor of a CONVENTIONS.md pattern doc.
  Skeptic argued speculative-files-without-trigger is worst-of-both-
  worlds. User adopted: ship zero stub agent files; document the
  pair-authoring shape in CONVENTIONS instead.

Both reframes surfaced via AskUserQuestion before any agent file was
authored (per **L-002**). **This is the substrate paying for itself
for the first time** — pre-design conversation produced design
corrections we'd otherwise have committed to in code and corrected
post-merge.

Phase 4 D1 (#71) shipped the substrate: `generator-base.md` (the
first write-capable agent role), `generator-css-codemod` + 
`evaluator-css-architecture` (the active pair), and the
PANEL-COMPOSITION extension (7-item precedence + three-way
tokens/naming/architecture boundary). D2 (#73) shipped the
CONVENTIONS pattern doc and an inline comment in
`Sketch.module.css` documenting the load-bearing `!important` +
`& canvas` carve-outs sketch-ideation flagged.

D3's contract is drafted and committed to
`ev.agent-guilds.domain-pairs-3` — the real CSS codemod exercise
that closes Phase 4. **Requires fresh Claude Code session per L-004**
(third observed instance of the pattern; the agents authored in D1
of Phase 4 in this session aren't in this process's registry). Target
file is `components/app/Sketch/Sketch.module.css` — the codemod
migrates the pre-existing `background-color: black;` named-color
literal to a token call while preserving the load-bearing carve-outs.

## Open threads

- **D3 ready to execute in fresh session**. After restart:
  `/ev-run agent-guilds` to orient onto
  `ev.agent-guilds.domain-pairs-3`, then spawn
  `generator-css-codemod` against `Sketch.module.css`, capture
  proposed artifact + resolved-value diff, spawn
  `evaluator-css-architecture` on the result, write results doc to
  `projects/2026-05-02-agent-guilds/codemod-exercises/4-sketch-module-css.md`.
  Contract is at
  `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.domain-pairs-3/01.md`
  with 12 ACs.
- **L-004 third observation strengthens existing learning**.
  Three data points now: panel-8 D8 (Phase 2 multi-evaluator
  smoke), whiteboard-3 D3 (Phase 3 design exercise), and
  domain-pairs-3 D3 (Phase 4 codemod exercise). All required
  Claude Code process restart. The pattern is now load-bearing
  substrate behavior; the original L-004 text should explicitly
  call out "process restart, not /clear" in the next
  `/griot-compact`. Worth elevating to body prose in
  `generator-base.md` / `evaluator-base.md` / `whiteboard-base.md`
  as a known-pattern callout, but that's a follow-up unit, not D3.
- **evaluator-tokens advisory-as-approved quirk** observed again in
  D2's panel. `evaluator-tokens` flagged the pre-existing
  `background-color: black` named-color literal in its text body
  but emitted `VERDICT: approved`, so `parse-and-aggregate`'s
  "approved → no findings" rule swallowed it from the structured
  advisory_findings list. Evaluators should emit `flagged` with
  `ADVISORY:` prefix to surface advisories cleanly. This is the
  same parser-leniency / evaluator-emit-pattern quirk surfaced in
  Phase 2 D8 and Phase 3 D3 — worth elevating to a substrate cleanup
  unit in Phase 5 or as a tactical Phase-1.5-style carryover.
- **/guild-whiteboard v1 per_agent_context redundancy** still open
  from Phase 3 D3's substrate finding. The SKILL prescribes
  per-engineer JSON map; in v1 the preamble is identical, making
  it ~6× wasteful. Brief-inlining is the pragmatic deviation; the
  cleanest fix is a `shared_round_context` field on `/guild-spawn`.
  Also worth carrying to a substrate cleanup unit.
- **Phase 5 (Griot integration + composability proof) is next-
  eligible after D3 merges**. The remaining phase per PLAN.md.

## Notes

- This session was heavy: two complete phases (Phase 3 closed, Phase
  4 nearly closed). Four PRs (#64, #71, #73, plus the in-flight D3).
  Substrate convention held throughout — prelude commits, AC-before-
  execution surfaced via AskUserQuestion, antagonist panel per unit.
- **The Phase 4 phase-start whiteboard was the high-value moment of
  the session.** Up until this PR, the always-on whiteboard had been
  validated mechanically (Phase 3 D3 exercised the pipeline against
  a non-contrived design topic) but hadn't yet caught a substantive
  design issue we'd otherwise have shipped wrong. Phase 4's reframe
  is the first concrete demonstration that the pre-design step pays
  for itself.
- Three substrate findings worth elevating in the next `/griot-
  compact`: L-004 strengthened with third data point, evaluator-
  tokens advisory-as-approved emitting pattern, /guild-whiteboard
  per_agent_context redundancy.
