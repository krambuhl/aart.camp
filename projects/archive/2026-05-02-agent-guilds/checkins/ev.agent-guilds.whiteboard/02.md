# Checkin 02 — ev.agent-guilds.whiteboard

**Created**: 2026-05-15 01:35
**Phase**: 3 — Whiteboard mechanism + engineers
**Unit**: D1 response — PR #55 feedback: framing change + skill-review pass

## Contract

- **Goal**: Address the two real review-comments on PR #55 in a
  single response unit:
  - **Item 1 (Blocker)**: flip the whiteboard step from "opt-in"
    to "always-on with sensible defaults" across `/ev-loop-*`
    SKILLs, `CONVENTIONS.md`, and the `guild-whiteboard` SKILL
    description. Phases that don't declare a `**Whiteboard**:`
    block in PLAN.md get a default panel (all
    available `whiteboard-*` engineers, topic derived from the
    phase name, 1 round). The PLAN block becomes an override,
    not a trigger. When NO `whiteboard-*` engineers are
    registered yet (bootstrapping case — D2 hasn't shipped),
    the loop logs a note and skips the whiteboard step rather
    than erroring.
  - **Item 2 (Suggestion)**: address `/review-skill`'s 1
    Critical and 3 Recommended findings against
    `.claude/skills/guild-whiteboard/SKILL.md`:
    1. **Critical**: add `disable-model-invocation: true` to
       the frontmatter. Apply family-wide (sibling
       `guild-spawn` and `guild-validate` SKILLs) for
       consistency — they have the same side-effect profile
       (parallel agent spawns) and the same internal-only
       intent (`user-invocable: false`).
    2. **Recommended**: add a worked-example section
       between Process and Contradiction detection.
    3. **Recommended**: pin the `per_agent_context` shape
       explicitly in Process step 4 (current prose leaves
       the data structure to the reader's inference).
    4. **Recommended**: fence the `whiteboard.ts append`
       bash invocation in Process step 6 (matches the
       fencing pattern in steps 2 and 3).

  This is a **response unit**, not a new deliverable — D1's
  scope was substrate-only (skill + script + test + ev-loop
  integration), and this unit only adjusts the work D1
  already did. The PR description will be re-authored via
  `/trout-pull-request` to reflect the new framing.

- **Acceptance criteria**:

  1. **Framing flip in `/ev-loop-interactive/SKILL.md`**:
     `### Whiteboard (opt-in)` → `### Whiteboard`. Section
     body rewritten to describe always-on default behavior:
     - The whiteboard step ALWAYS runs at phase start, before
       Step 1 (deliverable enumeration).
     - If the phase entry declares a `**Whiteboard**:` block,
       use the declared `engineers`, `topic`, `rounds` values.
     - Otherwise use defaults: `engineers` = all currently
       registered `whiteboard-*` agents (resolved via
       glob of `.claude/agents/whiteboard-*.md`), `topic` =
       the phase name, `rounds` = 1.
     - **Bootstrapping case**: if no `whiteboard-*` agents
       are registered, the loop logs a note ("no whiteboard
       engineers registered — skipping whiteboard step") and
       proceeds to Step 0. This is the case for any phase
       running before D2 ships the six engineers.
     - **L-004 session-boundary** note preserved: drop any
       newly-authored-this-session engineers from the
       effective list.

  2. **Framing flip in `/ev-loop-confidence/SKILL.md`**:
     parallel modification. Same section header rename,
     same body rewrite (with confidence-loop-appropriate
     pre-flight ordering).

  3. **Framing flip in `projects/CONVENTIONS.md`** § "PLAN.md
     phase-config extensions": rewrite to describe the
     always-on default + optional override semantics. The
     `**Whiteboard**:` block is now described as an override,
     not a trigger. Document the default-engineer resolution
     (glob of `.claude/agents/whiteboard-*.md`), the default
     topic (phase name), the default rounds (1), and the
     bootstrapping skip behavior.

  4. **Framing flip in `.claude/skills/guild-whiteboard/SKILL.md`**:
     the description line 11 currently says "opt-in pre-unit
     step." Update to "default pre-unit step" or equivalent.
     No process-step changes — the skill itself doesn't need
     to know about the opt-in vs opt-out framing; only the
     loops do.

  5. **`disable-model-invocation: true` added to three guild-*
     frontmatters**:
     - `.claude/skills/guild-whiteboard/SKILL.md`
     - `.claude/skills/guild-spawn/SKILL.md`
     - `.claude/skills/guild-validate/SKILL.md`
     All three are internal substrate primitives
     (`user-invocable: false`) composed by ev-loops and each
     other. The extra gate prevents auto-invocation by
     Claude based on description-keyword matches (e.g. "let's
     whiteboard the design"). Family-wide consistency: all
     three primitives get the same gate or none do.

  6. **Worked example added to
     `.claude/skills/guild-whiteboard/SKILL.md`** between
     `## Process` and `## Contradiction detection (v1:
     future-work)`. Format: an end-to-end "here's the input,
     here's the resulting artifact, here's the returned JSON"
     for a round-1 invocation against a 2-engineer panel.
     ≤ 30 lines.

  7. **`per_agent_context` shape pinned in Process step 4**:
     replace the current prose with an explicit JSON object
     structure (matching `/guild-spawn`'s
     `per_agent_context` input spec — an object keyed by
     engineer name, with each value being the prior-state
     preamble string).

  8. **Bash invocation fenced in Process step 6**: wrap the
     `node .claude/scripts/guild/whiteboard.ts append`
     invocation in a fenced bash block, matching the style
     of steps 2 and 3.

  9. **Verification**:
     - `npm run lint` (Biome) → exit 0 (no code changes;
       only `.md` and frontmatter edits).
     - `npm run build` (Next) → exit 0.
     - `npm test` (vitest) → exit 0; test count unchanged
       (18 files / 322 tests).
     - `git status --short` (fresh per **L-005**) shows only
       the expected new/modified paths.

  10. **D1 response panel** auto-derives to
      `evaluator-contract-fit, evaluator-naming` (same as D1's
      panel — all artifacts are `.md` substrate files except
      the response checkin itself, which is also `.md`). No
      manual override needed.

- **Rules applied** (re-verified at close):
  - `npm run lint` clean.
  - `npm run build` clean.
  - `npm test` passes; no new test cases authored (skill body
    + frontmatter changes only; no script behavior change).
  - **L-001** honored: no `tsx` invocations introduced.
  - **L-002** honored: surfaced the Item 1 fork to the user
    via AskUserQuestion before writing code; the always-on-
    with-defaults framing is the user's pick.
  - **L-005** honored: fresh `git status --short` before
    evaluator.
  - **L-009** honored: dense packet for the response panel.

- **Disqualifiers**:
  - No new `whiteboard-*` engineer agent files. D2 territory.
  - No real design-phase exercise. D3 territory.
  - No edits to `whiteboard.ts` or `whiteboard.test.ts`. The
    script behavior is unchanged; only the loops' policy
    around when to invoke it changes.
  - No edits to `/guild-spawn` or `/guild-validate` BODIES
    beyond the single-line frontmatter addition. The bodies
    themselves stay as-shipped — this unit's scope is
    consistency, not redesign.
  - No edits to `evaluator-*.md`, `PANEL-COMPOSITION.md`,
    `derive-panel.ts`, `parse-and-aggregate.ts`,
    `settings.json`, `package.json`, `biome.json`,
    `tsconfig.json`.
  - No edits to product code (`components/`, `sketches/`,
    `app/`, `tokens/`, `styles/`).
  - No `tsx` invocation anywhere.
  - No /review-skill Nice-to-have findings adopted (Required
    fields, output-format hoist, description trim, "No
    emojis." rule placement) — those are stylistic polish
    that don't gate this response unit. Recorded for
    consideration but not addressed.

- **Inputs**:
  - PR #55 review-comment thread (the two real comments).
  - `/review-skill` audit output (1 Critical + 3
    Recommended + 4 Nice-to-have findings).
  - `.claude/skills/guild-whiteboard/SKILL.md` (target of
    most edits).
  - `.claude/skills/ev-loop-interactive/SKILL.md`,
    `.claude/skills/ev-loop-confidence/SKILL.md` (framing
    flip).
  - `projects/CONVENTIONS.md` § PLAN.md phase-config
    extensions (framing flip).
  - `.claude/skills/guild-spawn/SKILL.md`,
    `.claude/skills/guild-validate/SKILL.md` (single-line
    frontmatter addition).
  - `response-02.md` (this unit's response plan, written by
    `/trout-pr-respond`).

## Execution

1. **Surfaced Item 1's fork to the user** via `AskUserQuestion`
   before any code changes (per **L-002** — the original PLAN.md
   Phase 3 spec said "opt-in"; flipping it is a real direction
   change worth surfacing). User picked **"Always-on with sensible
   defaults"** — every phase runs the whiteboard step
   automatically; PLAN.md block becomes an optional override
   layered on top of defaults.

2. **Ran `/review-skill`** on the current
   `.claude/skills/guild-whiteboard/SKILL.md` (before applying
   Item 1 framing change, so the audit is against the
   as-shipped D1 state). Captured 1 Critical + 3 Recommended
   + 4 Nice-to-have findings.

3. **Critical finding — `disable-model-invocation` family-wide
   pass, applied then reverted**. Reviewer's audit flagged the
   missing `disable-model-invocation: true` on
   `guild-whiteboard`'s frontmatter. Cross-checked siblings
   (`guild-spawn`, `guild-validate`) — neither had it either.
   Applied the gate to all three for family-wide consistency.
   **correction: reverted all three**. The next
   `/guild-validate` invocation in this very session immediately
   failed with `Skill guild-validate cannot be used with Skill
   tool due to disable-model-invocation` — the gate blocks
   composition-via-Skill, which is exactly the pattern the
   `guild-*` family is built on (`/guild-validate` composes
   `/guild-spawn`; `/ev-loop-*` composes `/guild-validate` and
   `/guild-whiteboard`; the whole substrate runs through
   programmatic Skill-tool invocation by Claude). The
   `review-skill` audit's Critical finding was correct for
   most user-facing skills but wrong for substrate-internal
   composition primitives. `user-invocable: false` is already
   the right gate: it keeps these skills off the user's
   `/<name>` autocomplete while leaving them composable from
   loops. Reverted all three `disable-model-invocation: true`
   additions; updated AC 5 of this contract to record the
   finding as a substrate-level observation rather than a
   change. This is a worthy learning candidate for the
   griot pipeline: substrate composition primitives
   intentionally require model-invocation, and the audit
   tool's recommendation shouldn't be applied blindly.

4. **Framing flip applied** across four files:
   - `.claude/skills/guild-whiteboard/SKILL.md` description
     (line 10-12): "opt-in pre-unit step" → "default pre-unit
     step (phases may override engineers/topic/rounds via
     PLAN.md `**Whiteboard**:` block)".
   - `.claude/skills/ev-loop-interactive/SKILL.md` (lines
     59-103): `### Whiteboard (opt-in)` → `### Whiteboard`.
     Body rewritten to document always-on behavior + default
     resolution (glob of `.claude/agents/whiteboard-*.md`,
     phase name as topic, rounds=1) + the PLAN.md override
     semantics + bootstrapping skip when no engineers
     registered. L-004 session-boundary note preserved
     (engineers list now derived from glob; same override
     pattern applies to the resolved list).
   - `.claude/skills/ev-loop-confidence/SKILL.md` (parallel
     lines 60-105): same shape.
   - `projects/CONVENTIONS.md` § "PLAN.md phase-config
     extensions": rewritten to describe always-on default +
     optional override, defaults enumerated, bootstrapping
     case documented.

5. **Recommended findings applied to
   `.claude/skills/guild-whiteboard/SKILL.md`**:
   - **R1 — worked example**: new `## Example` section
     between Process and Contradiction detection (≤ 30
     lines). Shows a round-1 invocation, the resulting
     whiteboard file, the returned JSON, and a brief note on
     round-2 semantics.
   - **R2 — `per_agent_context` shape pinned**: Process step
     4 expanded with an explicit JSON object example showing
     the per-engineer keyed-by-subagent_type structure, plus
     a note that the v1 body is identical for every engineer
     (only the routing surface differs).
   - **R3 — fenced bash in step 6**: the
     `whiteboard.ts append` invocation now sits in a fenced
     `bash` block, matching the style of steps 2 and 3.

6. **Nice-to-have findings not addressed in this unit** (per
   contract disqualifier). Recorded for future polish: arg
   "Required" badges in Inputs, output-format reorder, long
   description trim, "No emojis." rule placement.

7. **Pre-verification**. `npm run lint`, `npm run build`,
   `npm test` all green. Test count unchanged at 18 files /
   322 tests — no code changes in this unit; pure
   markdown/frontmatter edits.

8. **Antagonist panel**. Derive-panel on the response unit's
   file list returns `evaluator-contract-fit` only — all
   modified files classify as `.md` (skill bodies, project
   docs, checkin/response files). No naming evaluator
   triggered since no `.ts` files changed. Single-evaluator
   panel.

## Scope

- **Created**:
  - `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.whiteboard/02.md`
    (this checkin).
  - `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.whiteboard/response-02.md`
    (the response plan, written by `/trout-pr-respond`).

- **Modified**:
  - `.claude/skills/guild-whiteboard/SKILL.md` — description
    framing flipped ("opt-in" → "default"); worked example
    section added between Process and Contradiction
    detection; `per_agent_context` shape pinned in Process
    step 4; bash invocation fenced in Process step 6.
  - `.claude/skills/ev-loop-interactive/SKILL.md` —
    `### Whiteboard (opt-in)` section renamed and rewritten
    to describe always-on default + optional override.
  - `.claude/skills/ev-loop-confidence/SKILL.md` — parallel.
  - `projects/CONVENTIONS.md` § "PLAN.md phase-config
    extensions" — rewritten to describe always-on default +
    override + bootstrapping case.

- **Not modified (despite initial intent — see Execution § 3
  for revert rationale)**:
  - `.claude/skills/guild-spawn/SKILL.md`
  - `.claude/skills/guild-validate/SKILL.md`

- **Not touched** (per disqualifiers):
  - `whiteboard.ts`, `whiteboard.test.ts` — script behavior
    unchanged; only the loop's invocation policy changed.
  - No edits to `/guild-spawn` or `/guild-validate` body
    prose beyond the single-line frontmatter addition.
  - No new engineer agent files (D2 scope).
  - No design-phase exercise (D3 scope).
  - No edits to evaluator-* agents, PANEL-COMPOSITION.md,
    derive-panel.ts, parse-and-aggregate.ts, settings.json,
    package.json, biome.json, tsconfig.json, product code.

## Pre-computed verification (authoritative)

- `npm run lint` (Biome) → exit 0; "Checked 129 files in
  32ms. No fixes applied. Found 2 warnings." (pre-existing
  dereferenced-symlink warnings unchanged).
- `npm run build` (Next) → exit 0; static + SSG routes
  prerendered.
- `npm test` (vitest) → exit 0; **18 files / 322 tests
  passed** in 4.18s (unchanged from D1 — no code edits).
- `git status --short` (fresh per **L-005**, post-revert) →
  4 modified (3 skill `.md` + CONVENTIONS.md) + 2 untracked
  (the new checkin 02 + response-02 plan). Tree otherwise
  clean. `.claude/skills/guild-spawn/SKILL.md` and
  `.claude/skills/guild-validate/SKILL.md` are NOT modified —
  the `disable-model-invocation` Critical finding was tried
  and reverted (see Execution § 3).
- `derive-panel.ts` on the artifact file list →
  `evaluator-contract-fit` (validated by dogfood per
  **L-013**). Single-evaluator panel; no `.ts` files
  changed.

## Evaluator verdict

**Panel**: `evaluator-contract-fit` only. No manual
override; the derive-panel script produced this output
directly from the response unit's file list (all `.md`).

**Run 1 of 1**: see § Notes for the PR. contract-fit gate
cleared.

## Notes for the PR

- **PR #55 review-comment Item 1 addressed**: whiteboard step
  flipped from opt-in to always-on with sensible defaults. User
  picked the framing via AskUserQuestion (per **L-002**) — the
  PLAN.md `**Whiteboard**:` block is now an optional override
  layered on top of always-on defaults (engineers via glob of
  `.claude/agents/whiteboard-*.md`, topic = phase name,
  rounds = 1). Bootstrapping case (no engineers registered)
  logs a one-line note and skips. Documented in both `/ev-loop-*`
  SKILLs and in `projects/CONVENTIONS.md`.

- **PR #55 review-comment Item 2 addressed**: ran
  `/review-skill` against the new `guild-whiteboard/SKILL.md`.
  Audit returned 1 Critical + 3 Recommended + 4 Nice-to-have
  findings. Adopted: the 3 Recommended (worked example,
  `per_agent_context` shape pinned, fenced bash invocation).

- **correction: apply-then-revert on the
  `disable-model-invocation` Critical finding** (load-bearing
  observation, worth elevating to a learning candidate for
  the griot pipeline):
  - The reviewer's `/review-skill` flagged the missing
    `disable-model-invocation: true` on `guild-whiteboard`'s
    frontmatter as Critical. The rationale was reasonable —
    the skill has side effects (file writes, parallel agent
    spawns) and intent is internal-only.
  - I applied the gate family-wide to all three `guild-*`
    SKILLs for consistency.
  - The next `/guild-validate` invocation in this same
    session failed with `Skill guild-validate cannot be used
    with Skill tool due to disable-model-invocation`. The
    gate blocks Skill-tool composition — which is exactly
    the substrate pattern the `guild-*` family is built on
    (`/guild-validate` composes `/guild-spawn`; `/ev-loop-*`
    composes `/guild-validate` and `/guild-whiteboard`; the
    whole substrate flows through programmatic Skill-tool
    invocation).
  - Reverted all three `disable-model-invocation: true`
    additions. `user-invocable: false` is already the right
    gate — it keeps these skills off the user's `/<name>`
    autocomplete while leaving them composable from loops.
  - **Generalizable observation**: `/review-skill`'s
    Critical recommendations are correct for user-facing
    skills but wrong for substrate-internal composition
    primitives. Future `/review-skill` invocations on the
    `guild-*` family (or any future composition primitive
    family) should treat `disable-model-invocation: true`
    as advisory-not-blocking, contingent on whether the
    skill is composed by other skills via the Skill tool.

- **correction: contract AC 5 not rewritten in-place**.
  Per the evaluator's hygiene note: the checkin's Contract
  AC 5 still describes the original family-wide pass as
  the intended change; the reframe-to-substrate-finding
  lives in Execution § 3 and the "Not modified" Scope
  subsection. Future contract authors should consider
  rewriting an AC in-place when a mid-flight correction
  reframes it, rather than relying on Execution+Scope to
  carry the reframe. Recording this as a process note —
  the actual artifact matches the reframed version, so no
  rework of the substrate.

- **Nice-to-have findings deferred** (4 items from
  `/review-skill`):
  - "Required" badges in Inputs section.
  - Output-format section hoist closer to Inputs.
  - Long description trim.
  - "No emojis." rule placement.

  Recorded for future polish; not in this response unit's
  scope.

- **L-001 honored**: no `tsx` invocations introduced.
- **L-002 honored**: surfaced the Item 1 fork (always-on
  flavor) via AskUserQuestion before any code changes;
  surfaced the Critical-finding apply-then-revert as a
  substrate observation in real time.
- **L-005 honored**: re-ran `git status --short` fresh
  immediately before the evaluator panel; recorded output
  verbatim in § Pre-computed verification (including the
  post-revert state confirmation).
- **L-009 honored**: dense packet with pre-computed
  verification, paraphrased contract with file:line
  mappings, iteration story explicitly noting the
  apply-then-revert.
- **L-013 honored**: every claim in this checkin cites
  actual artifact state (the `disable-model-invocation`
  finding was confirmed by an actual `/guild-validate`
  failure in this session, not inferred from documentation).
