# Checkin 14 — ev.agent-guilds.phase-1-5-substrate-cleanup-9

**Created**: 2026-05-09 22:10
**Phase**: 1.5 — Substrate primitive cleanup
**Unit**: End-to-end verification of migrated Phase 1.5 substrate + remove orphaned `autosave.ts --init` (deliverable 12; closes Phase 1.5)

## Contract

**Goal**: Verify the Phase 1.5 migrated substrate scripts compose correctly into a real project lifecycle (scaffold → autoload → save-session → archive). Exercise the four script primitives end-to-end against a throwaway test project. Remove the now-orphaned `autosave.ts::runInit` + `--init` flag (deferred from D10's AC #3) and the four `--init` tests in `autosave.test.ts`. Mark Phase 1.5 complete via autosave. The exercise is mechanical: invoke the SCRIPTS directly (not the SKILLS) so the verification is reproducible and doesn't recurse the loop infrastructure. The artifact is a small archived test project at `projects/archive/2026-05-09-phase-1-5-test/` plus the autosave cleanup.

**Acceptance criteria**:

1. **Scaffold a throwaway test project at `projects/2026-05-09-phase-1-5-test/`** by direct invocation of `.claude/scripts/trout/plan-scaffold.ts`. Provide three minimal temp files:
   - `/tmp/d12-test-plan.md` — a small PLAN.md with one phase and one deliverable.
   - `/tmp/d12-test-config.md` — a config.md with default values (verification command, base branch, no reviewers/labels, worker bindings).
   - `/tmp/d12-test-manifest-init.json` — `{title, started:"2026-05-09", strategy, phases:[{name:"Verify"}]}`.
   - **Verify**: project directory exists with `PLAN.md`, `config.md`, `MANIFEST.md`, `sessions/`, `checkins/` subdirs. Spot-check that PLAN.md content matches the temp file byte-for-byte.

2. **Exercise `autoload.ts`** on the test project: `node .claude/scripts/trout/autoload.ts phase-1-5-test`. **Verify**: the briefing prints to stdout, contains the project's title, status (`active`), the seeded phase row, and the "no checkins yet" / "no PRs yet" state.

3. **Manually write a unit checkin for the test project at `projects/2026-05-09-phase-1-5-test/checkins/ev.phase-1-5-test.verify/01.md`.** The checkin documents: contract (verification goal), execution (mechanical), evaluator note (N/A — verification is mechanical, the parent checkin's pre-computed verification IS the evidence). NOT invoking `/ev-loop-interactive` (avoids recursive Skill orchestration; the substrate primitives D5/D6/D9 have their own direct test coverage and don't need re-verification through a contrived dummy unit). Single checkin, numbered 01.

4. **Record a checkin-created event** on the test project's MANIFEST via `autosave.ts --event=checkin-created --detail='01 on ev.phase-1-5-test.verify' --phase-update=1:in-progress:branch=ev.phase-1-5-test.verify:checkin=01`. **Verify**: the test project's MANIFEST shows the event row and the phase row updated.

5. **Exercise `save-session-finalize.ts`** on the test project: `node .claude/scripts/trout/save-session-finalize.ts phase-1-5-test --content-file=/tmp/d12-test-session.md --date=2026-05-09`. The session content file documents the verification exercise. **Verify**: a file appears at `projects/2026-05-09-phase-1-5-test/sessions/2026-05-09-a.md` with the provided content; MANIFEST gets a `session-saved` event row.

6. **Exercise `archive-relocate.ts`** on the test project: `node .claude/scripts/trout/archive-relocate.ts phase-1-5-test`. **Verify**:
   - Project directory moved from `projects/2026-05-09-phase-1-5-test/` to `projects/archive/2026-05-09-phase-1-5-test/`.
   - Manifest's `**Status**` field flipped from `active` to `archived`.
   - The original `projects/2026-05-09-phase-1-5-test/` no longer exists.

7. **Remove the `autosave.ts --init` code path** (deferred from D10):
   - Delete `runInit` function (~50 lines, autosave.ts:378-461 in current numbering).
   - Delete `resolveInitTarget` function (~10 lines, autosave.ts:144-153).
   - Delete `InitDetail` type definition.
   - Remove the `--init` boolean from `parseArgs` options.
   - Remove `args.init` field from `Args` type.
   - Remove the `--init` branch in `main()` and the related validation (`--init does not accept --event/--current-state/--phase-update`).
   - Update `ARG_HINT` constant to drop `[--init]`.
   - **Net reduction**: ~70 lines from `autosave.ts`. Final line count: under ~410 (was 476).

8. **Remove `--init`-specific tests from `autosave.test.ts`**:
   - Lines 281-336 (4 tests: `--init scaffolds...`, `--init refuses if dir exists`, `--init rejects malformed JSON`, `--init combined with --event is rejected`).
   - Net reduction: ~55 lines from `autosave.test.ts`. Test count drops by 4.
   - Keep the `project-initialized` event vocabulary entries (line 52, line 93) — that event is still part of the substrate vocabulary, just emitted by `plan-scaffold.ts` now instead of `autosave --init`.

9. **Mark Phase 1.5 complete** on the agent-guilds project via `autosave.ts --event=phase-completed --detail=1.5 --phase-update=1.5:completed`. The phase row's status flips from `in-progress` to `completed`.

10. **Verification** (the parent checkin's pre-computed verification, which IS the e2e evidence):
   - `npm run lint` clean.
   - `npm run build` clean.
   - `npm run test` passes — was 214 (post-D11). Expect **210** (-4 from `--init` test removal). 13 test files (down from 14? — check whether autosave.test.ts still has tests after removal; should still have 16-ish remaining tests).
   - `test -d projects/2026-05-09-phase-1-5-test` returns false (project relocated to archive).
   - `test -d projects/archive/2026-05-09-phase-1-5-test` returns true.
   - `grep "Status: archived" projects/archive/2026-05-09-phase-1-5-test/MANIFEST.md` matches.
   - `wc -l .claude/scripts/trout/autosave.ts` reports under ~410 (down from 476; ~66+ lines saved).
   - `wc -l .claude/scripts/trout/autosave.test.ts` reports under ~295 (down from 348; ~53 lines saved).
   - `grep -n "runInit\|resolveInitTarget\|InitDetail" .claude/scripts/trout/autosave.ts` returns no matches.
   - `grep -n "'--init'\|--init" .claude/scripts/trout/autosave.ts` returns no matches outside of comments documenting the migration.
   - Phase 1.5 row in `agent-guilds/MANIFEST.md` shows `completed` status with cleanup-9 / 14 / `#<TBD>` once PR opens.
   - `## Events` in `agent-guilds/MANIFEST.md` includes a `phase-completed | 1.5` event row.

11. **No co-located refinements bundled.** Same five session-b items still deferred (autoload-gh reconciliation, commit-allowlist tightening, submit pr-number sanity check, autosave-phase-update-Current-branch propagation, submit-N-placeholder substitution). CONVENTIONS.md `node:test` line stays stale (eligible for a tiny standalone doc-sweep PR after Phase 1.5 closes; not in D12 scope). `resolveProject` four-times-duplicated continues to be tracked. `Item.location` overload from D9 not addressed.

**Rules applied**:

- Substrate-script convention: D12 verifies all migrated CRUD scripts compose into a real project lifecycle.
- Cleanup discipline: deferred work (D10's `autosave --init` removal) lands in the same PR as the verification that proves the new path works. Done together, not as a chain of trailing follow-ups.
- Test layout: existing autosave.test.ts loses 4 cases tied to deleted code; remaining tests (16+) continue passing unchanged.
- Closer-PR discipline: D12 is the closing PR for Phase 1.5; it includes the phase-completed event and the remaining cleanup. After this merges, Phase 1.5 is done and Phase 2 can start.

**Disqualifiers**:

- **Test project NOT scaffolded via `plan-scaffold.ts`.** Manual `mkdir` + `writeFileSync` defeats the purpose; the script must be the path.
- **Test project NOT archived via `archive-relocate.ts`.** Manual `mv` defeats the purpose.
- **Session note NOT written via `save-session-finalize.ts`.** Manual `writeFileSync` defeats the purpose.
- **`autosave.ts --init` code retained after this checkin.** The whole point of the deferred-cleanup decision was to remove it once the new path was proven. Now is now.
- **`autosave.test.ts` retains tests against the deleted `--init` path.** Tests for code that no longer exists are dead weight; remove them.
- **Phase 1.5 NOT marked complete via `autosave --event=phase-completed`.** Closing the phase is part of D12.
- **A real GitHub PR opened for the test project.** The test project lives entirely on cleanup-9 (no separate branch push, no separate PR). It gets one PR — the D12 PR for cleanup-9 itself.
- **Real evaluator subagent run for the test project's dummy unit.** Over-ceremony for a verification dummy; the substrate's evaluator path was already exercised in checkins 12 and 13 of this same arc.
- **Test project's MANIFEST modified by hand instead of through `autosave.ts` invocations.** The whole point is to exercise the script paths.
- **Sibling project (agent-guilds, griot-subagents, etc.) state polluted** by the test project's existence or its archive.
- **`project-initialized` event vocabulary entries removed from `autosave.test.ts`.** The event is still in the substrate vocabulary; it's just emitted by `plan-scaffold.ts` now. Remove only the `--init`-specific tests, not the vocabulary references.
- **Substrate refinements bundled in.**

**Inputs**:

- All migrated substrate scripts under `.claude/scripts/{trout,griot}/`.
- `.claude/scripts/trout/autosave.ts` — the cleanup target (lines to remove identified in AC #7).
- `.claude/scripts/trout/autosave.test.ts` — lines 281-336 to remove (the four `--init` tests).
- D7 `archive-relocate.ts`, D8 `save-session-finalize.ts`, D10 `plan-scaffold.ts` — exercise targets.
- D3 `autoload.ts` — exercise target (briefing read).
- `projects/2026-05-02-agent-guilds/PLAN.md` § Phase 1.5 deliverable 12.
- `projects/CONVENTIONS.md` § Substrate scripts, § Skills as interfaces vs workers.

## Execution

1. **Prepare temp inputs.** Wrote `/tmp/d12-test-plan.md`, `/tmp/d12-test-config.md`, `/tmp/d12-test-manifest-init.json` for the test project's scaffold.
2. **Scaffold test project (D10 path).** `node .claude/scripts/trout/plan-scaffold.ts 2026-05-09-phase-1-5-test --plan-file=... --config-file=... --manifest-init-file=...` → `plan-scaffold-written: projects/2026-05-09-phase-1-5-test/`. Verified: PLAN.md, config.md, MANIFEST.md, sessions/, checkins/ all created with expected content.
3. **Exercise autoload (D3 path).** `node .claude/scripts/trout/autoload.ts phase-1-5-test` → briefing renders correctly with title "Phase 1.5 e2e verification test project", status `active`, single phase row "Verify | not-started", and "Phase 1 (Verify) is ready to start" suggestion.
4. **Author test project's checkin 01.** Wrote `projects/2026-05-09-phase-1-5-test/checkins/ev.phase-1-5-test.verify/01.md` documenting the verification exercise. Recorded `checkin-created` event via autosave (D2 path). Phase row advances to `cleanup-1 / 01 / —`. **Latest checkin** field updates correctly.
5. **Exercise save-session-finalize (D8 path).** `node .claude/scripts/trout/save-session-finalize.ts phase-1-5-test --content-file=/tmp/d12-test-session.md --date=2026-05-09` → `session-saved: projects/2026-05-09-phase-1-5-test/sessions/2026-05-09-a.md`. Verified session file written with correct content.
6. **Exercise archive-relocate (D7 path) — FIRST ATTEMPT FAILED.** Initial run errored with `archive-relocate-error: git mv failed: fatal: source directory is empty`. Root cause: `archive-relocate.ts` uses `git mv`, which requires source files to be tracked. My scaffold step left them untracked. **Fix**: committed the test project first ("Scaffold throwaway phase-1-5-test project for D12 verification"), then re-ran archive-relocate. **Second attempt succeeded**: `relocated: ... → projects/archive/2026-05-09-phase-1-5-test`. Verified: source dir gone, archived dir present, MANIFEST `**Status**: archived`. **Substrate finding**: `archive-relocate.ts` requires git-tracked source. The "real" substrate workflow always has tracked sources by the time archive runs (every checkin commits as it's authored), so this is an implicit invariant rather than a bug. Worth documenting in CONVENTIONS.md or the script's own docstring as a follow-up. **Second substrate finding from D12**: `archive-relocate.ts` modifies the manifest AFTER `git mv`, leaving the Status field flip unstaged. Substrate-refinement candidate (modify-then-mv would keep rename + content change in one staged unit). Captured as a separate "Flip phase-1-5-test MANIFEST Status to archived" commit; flagged in commit message body.
7. **Remove `autosave.ts --init` code path** (deferred from D10):
   - Deleted `runInit` function (~85 lines, lines 355-438 in pre-D12 numbering).
   - Deleted `resolveInitTarget` function (~10 lines).
   - Deleted `InitDetail` type definition.
   - Removed `init: { type: 'boolean' }` from parseArgs options.
   - Removed `init` field from Args type and its `--init`-related validation lines.
   - Updated ARG_HINT: removed `[--init]`; tightened required `--event=<name>` framing (no longer "(unless --init)").
   - Removed `--init` branch in `main()`.
   - Removed `mkdirSync` and `dirname` from imports (no longer used after `runInit` deletion).
   - **Net script reduction**: 476 → 362 lines (-114 lines, more than the ~70 estimated; the runInit body had a long inline manifest template that absorbed more lines than expected).
8. **Remove `--init` tests from `autosave.test.ts`** (lines 281-336 in pre-D12 numbering):
   - Deleted 4 tests: `--init scaffolds...`, `--init refuses if dir exists`, `--init rejects malformed JSON`, `--init combined with --event is rejected`.
   - **Net test reduction**: 348 → 291 lines (-57 lines, -4 tests).
   - Kept the two `project-initialized` event vocabulary references (lines 52, 93 in current file) — that event is still emitted by `plan-scaffold.ts`.
9. **Mark Phase 1.5 complete.** `node .claude/scripts/trout/autosave.ts agent-guilds --event=phase-completed --detail=1.5 --phase-update=1.5:completed` → autosave records the event and flips Phase 1.5 row from `in-progress` to `completed`. Verified row reads: `| 1.5 | Substrate primitive cleanup | completed | ev.agent-guilds.phase-1-5-substrate-cleanup-9 | — | — |`.
10. **Verify.** `npm run lint` clean. `npm run build` clean. `npm run test` reports **210/210 passing across 14 test files** (was 214 pre-D12; -4 from `--init` test removal, exactly as expected).

## Scope

Files created:
- `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.phase-1-5-substrate-cleanup-9/14.md` (this checkin)
- `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.phase-1-5-substrate-cleanup-9/14-packet.md` (the dense packet)
- `projects/archive/2026-05-09-phase-1-5-test/PLAN.md`
- `projects/archive/2026-05-09-phase-1-5-test/config.md`
- `projects/archive/2026-05-09-phase-1-5-test/MANIFEST.md`
- `projects/archive/2026-05-09-phase-1-5-test/sessions/2026-05-09-a.md`
- `projects/archive/2026-05-09-phase-1-5-test/checkins/ev.phase-1-5-test.verify/01.md`
  (the test project, archived in place — created by `plan-scaffold.ts`, exercised through scripts, then relocated to `projects/archive/` by `archive-relocate.ts`)

Files modified:
- `.claude/scripts/trout/autosave.ts` (476 → 362 lines; -114 net). Removed `runInit`, `resolveInitTarget`, `InitDetail` type, `--init` flag handling, two unused imports.
- `.claude/scripts/trout/autosave.test.ts` (348 → 291 lines; -57 net). Removed 4 `--init`-specific tests; kept `project-initialized` event vocabulary references.
- `projects/2026-05-02-agent-guilds/MANIFEST.md` — branch reconciliation (cleanup-9 cut, phase row reset, current state narrative refresh) plus `phase-completed | 1.5` event and phase row flipped to `completed`.

External effects:
- `autosave.ts --init` flag is gone. Any external caller of it would now get `--event=<name> is required`. The only caller in this repo was `/trout-plan` SKILL.md, which D10 already swapped to `plan-scaffold.ts`. No other callers detected.

## Changes since previous checkin

Net delta from `13.md` (PR #32, merged):
- Test project artifact created in `projects/archive/` documenting the e2e verification exercise.
- `autosave.ts` shrunk by 114 lines (orphaned `--init` path removed).
- `autosave.test.ts` shrunk by 57 lines (4 `--init` tests removed).
- `MANIFEST.md` records `phase-completed | 1.5`; phase row flipped to `completed`.
- Phase 1.5 closed. Phase 2-5 are now eligible to start.

## Evaluator verdict

Approved on panel run 1 of 1 (single-evaluator panel `evaluator-contract-fit`). Spot-check on `projects/archive/2026-05-09-phase-1-5-test/` confirmed the archive artifact exists with the full expected file tree (PLAN.md, config.md, MANIFEST.md, checkins/, sessions/) and `**Status**: archived` — collapsing ACs #1-#6 into one observation (every migrated script left its expected artifact). Spot-check on `autosave.ts` confirmed zero references to `runInit`/`resolveInitTarget`/`InitDetail`/`--init` outside of a single comment at line 30 documenting the historical context (preserved per disqualifier guidance). Cross-check of remaining ACs against pre-computed verification all clean: AC #8 (test cleanup with vocabulary refs preserved at lines 52/93), AC #9 (Phase 1.5 row reads completed; phase-completed event row recorded), AC #10 (210/210 tests, line counts under all caps), AC #11 (two substrate findings logged as Notes for follow-up rather than addressed in-line). Disqualifier sweep clean: the bundled `--init` cleanup is a named deliverable per AC #7, not an opportunistic refinement. One minor advisory observation (not flagged): the contract claims `autosave.test.ts:281-336` were the removed lines, which the evaluator didn't verify directly but is consistent with the programmatically-verified -4 test / -57 line deltas.

## Notes for the PR

- **Substrate finding 1 (`archive-relocate.ts` requires git-tracked source).** First attempt at archive-relocate failed with `git mv: fatal: source directory is empty`. The script uses `git mv` (which only operates on tracked files) but doesn't document this requirement. Real substrate workflow naturally satisfies this (every checkin commits before the next operation), so it's an implicit invariant rather than a bug. Worth documenting in `archive-relocate.ts`'s docstring or in CONVENTIONS.md. Out of scope to fix in D12; flagged as a tiny doc-sweep follow-up.
- **Substrate finding 2 (`archive-relocate.ts` leaves Status flip unstaged).** The script does `git mv` first, then writes the modified manifest at the new location. Result: rename is staged, but the Status `active → archived` change is left unstaged in the working tree. The user has to commit it as a separate operation. **Substrate-refinement candidate**: modify-then-mv would keep the rename + content change in one staged unit. Captured as a separate "Flip phase-1-5-test MANIFEST Status to archived" commit on this branch; the commit message documents the finding. Out of scope to fix in D12.
- **`--init` removal saved more lines than estimated** (-114 vs. estimated ~70). The runInit body had a long inline manifest template that I had estimated at ~50 lines but was actually ~85. The total cleanup is one of the more satisfying compressions of the project (a third of autosave.ts gone, all dead code).
- **Phase 1.5 closes here.** The arc has been: 12 deliverables across 9 PRs (#9, #10, #13, #16, #18, #23, #26, #28, #30, #32, #<this PR>). Two co-shipped follow-ons outside the plan (#12, #20). One sibling project (`griot-subagents`) shipped in parallel without conflict. Phase 2 (antagonist evaluator panel with full antipattern catalogs and CLI validators) is now eligible. The substrate underneath is in shape: every CRUD primitive is a script under `.claude/scripts/<family>/`, every LLM-shaped skill body composes those scripts via Bash, and the convention is well-trodden after 12 deliverables of practice.
- **Substrate refinement backlog carried forward into Phase 2 / future maintenance:**
  - The five session-b items (autoload-gh reconciliation, commit-allowlist tightening, submit pr-number sanity check, autosave-phase-update Current-branch propagation, submit-N-placeholder substitution).
  - CONVENTIONS.md line 65 `node:test` text vs. actual practice (vitest with `// @vitest-environment node`).
  - `resolveProject` four-times-duplicated across the trout family (plan-scaffold doesn't use it but the four prior duplications remain copy-pasted).
  - `Item.location` overload in `pr-respond-plumbing.ts` (D9).
  - The two new D12 findings on `archive-relocate.ts` above.
  - All eligible for tiny standalone follow-up PRs after Phase 1.5 lands or as part of Phase 2's setup.
- **No co-located refinements addressed.** D12's discipline is the same as D7-D11.

