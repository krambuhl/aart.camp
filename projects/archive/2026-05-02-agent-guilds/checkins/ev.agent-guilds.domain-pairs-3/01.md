# Checkin 01 — ev.agent-guilds.domain-pairs-3

**Created**: 2026-05-15 12:35
**Phase**: 4 — Domain pairs
**Unit**: D3 — Real CSS codemod exercise (Phase 4 close)

## Contract

- **Goal**: Run the v1 active generator-evaluator pair
  (`generator-css-codemod` + `evaluator-css-architecture`)
  end-to-end against a real `.module.css` artifact. Verify
  PLAN.md § Phase 4's verification clause: *"CSS codemod
  phase runs through the active pair; phase config naming a
  stub generator errors loudly with activation criteria."*
  With this unit's checkin merged, Phase 4 closes — all three
  Phase 4 deliverables (D1 substrate, D2 pattern docs, D3
  exercise) shipped.

  **Critical pre-condition — fresh Claude Code session
  required**. The `generator-css-codemod` and
  `evaluator-css-architecture` agents were authored in D1 of
  Phase 4 (PR #71, merged in the session that opened Phase 4)
  and the `evaluator-css-architecture` precedence-list entry
  + three-way boundary doc were authored in the same D1. Per
  the strengthened **L-004**, those agents are NOT callable
  in the Claude Code process they were authored in — the
  agent registry loads once per process start, and `/clear`
  is NOT a session boundary. D3 must run in a Claude Code
  process started AFTER #71 merged. This is the third
  observed instance of the L-004 split pattern (after
  panel-8 D8 for Phase 2 and whiteboard-3 D3 for Phase 3) —
  it's now a load-bearing substrate pattern, not an
  edge case.

  **The PLAN.md verification clause's "stub generator errors
  loudly" subclause does NOT apply** to D3 directly. The
  Phase 4 whiteboard adopted reframe (D1 of Phase 4) dropped
  the three stub agent files in favor of a CONVENTIONS.md
  pattern doc (which shipped in D2). There are no stub agent
  files in the codebase; the "loud error" path is now a
  conceptual extension (the loop should error on phase-config
  naming of non-existent agents), but that's general handling
  and is not exercised by this unit. D3's verification scope
  is the first subclause only: *"CSS codemod phase runs
  through the active pair."*

  This contract is authored in the current session (which can
  freely draft prose). The execution work (generator spawn,
  evaluator spawn, file mutations, captured verdicts) happens
  in the next session.

- **Target file**:
  `components/app/Sketch/Sketch.module.css`.

  Rationale: this file is the v1 codemod's natural test
  surface. It contains:
  - A pre-existing `background-color: black;` named-color
    literal (line 17) — the migration target.
    `evaluator-tokens` flagged this in D2's panel as a
    `tokens-named-color` advisory; D3 is the codemod that
    addresses it.
  - The two load-bearing patterns documented in D2's inline
    comment + the `generator-css-codemod` body's
    "Sketch-CSS carve-outs": `!important` on canvas
    width/height (lines 18-19) and the `& canvas` descendant
    selector. The codemod **must not normalize these away**.
  - The agent-level carve-out documentation lives in
    `.claude/agents/generator-css-codemod.md` § "Sketch-CSS
    carve-outs"; D3 exercises whether the generator actually
    respects its own documented constraint.

- **Acceptance criteria**:

  1. **`generator-css-codemod` invoked via `Agent` tool**
     with `subagent_type: generator-css-codemod` and a brief
     containing:
     - The unit contract for D3 (this checkin's Goal +
       Acceptance criteria, paraphrased).
     - The explicit target file:
       `components/app/Sketch/Sketch.module.css`.
     - The intended transformation:
       `background-color: black;` → a `token(...)` call.
       The exact token name is the generator's call — the
       file's surrounding context (canvas backdrop on a
       sketch wrapper) suggests something like
       `token("bg.inverted.default")` would fit aart.camp's
       token namespace; `token("color.canvas")` or a
       sketch-specific token would also work if it exists.
       The generator should examine `tokens/tokens.ts` /
       `tokens/design-tokens.json` for available tokens.

  2. **The generator's output respects the sketch-CSS
     carve-outs documented in its body**:
     - `!important` on `height: auto;` and `width: 100%;`
       (lines 18-19 of the current file) — preserved.
     - The `& canvas` descendant selector — preserved (NOT
       rewritten to a separate class).
     - The D2-added inline comment block — preserved (the
       generator should not delete or modify the load-
       bearing-patterns documentation).

  3. **The generator's output includes a resolved-value
     diff table** per `generator-base.md`'s output contract
     (visual properties require this). Table maps
     `background-color: black;` (before, resolves to
     `#000000`) → `background-color: token("<chosen>")` (after,
     resolves to the token's actual hex value).

  4. **The generator's Scope summary lists exactly one file
     modified**:
     `components/app/Sketch/Sketch.module.css`. No other
     files. Per `generator-css-codemod`'s body, the generator
     is narrow-scope (per-file or small-set); D3 names one
     file, so the generator must not sweep other CSS
     modules.

  5. **`evaluator-css-architecture` invoked via `Agent` tool**
     with the proposed codemod output as the artifact under
     review. The specialist evaluator's verdict is captured
     verbatim into the results doc. Expected: `approved` with
     possible advisory observations (e.g. the chosen token
     name's semantic fit, codemod-specific rubric checks
     like resolved-value-diff-present and sketch-CSS-carve-
     outs-respected).

  6. **D3 results doc** at
     `projects/2026-05-02-agent-guilds/codemod-exercises/4-sketch-module-css.md`
     captures:
     - The exact `Agent` invocations (subagent_type + brief)
       for both `generator-css-codemod` and
       `evaluator-css-architecture`.
     - The generator's proposed artifact (the file diff +
       Scope + Execution narrative).
     - The resolved-value diff table the generator produced.
     - The evaluator's verdict verbatim.
     - A "Phase 4 verification" section cross-referencing
       the PLAN.md verification clause's first subclause with
       observed evidence.
     - A "Substrate findings" section if any pipeline quirks
       surface during execution.

  7. **The codemod is APPLIED to the target file**. The
     generator's tools allowlist includes `Edit`/`Write`
     per `generator-base.md`; the generator produces both
     the planned migration AND executes it on disk. D3's
     `git status` will show
     `components/app/Sketch/Sketch.module.css` modified
     with the new `token(...)` value plus the preserved
     carve-outs.

  8. **`npm run build` passes after the codemod**. The token
     name the generator chose must resolve (the build
     compiles `.module.css` via the project's PostCSS
     `token()` function, which will fail if the token doesn't
     exist). If the build fails, that's a regression — D3
     surfaces it; the generator's verdict is flagged.

  9. **`evaluator-tokens` no longer flags the migrated
     line** — running `derive-panel.ts` on the modified file
     yields the same evaluator list, but tokens evaluator's
     would-be advisory `tokens-named-color` on line 17 no
     longer applies (the literal is now a token call). The
     codemod's purpose is satisfied.

  10. **L-004 acknowledgment in Notes for PR**: cite the
      session-restart timeline. This is the **third**
      observed L-004 split, joining panel-8 D8 (Phase 2
      multi-evaluator smoke) and whiteboard-3 D3 (Phase 3
      design exercise) — three data points elevate L-004 to
      a load-bearing substrate pattern that should be
      mentioned explicitly in `generator-base.md` /
      `evaluator-base.md` body prose for future authors. (D3
      doesn't add that prose; that's a follow-up unit. D3
      just cites the pattern.)

  11. **Verification**:
      - `npm run lint` (Biome) → exit 0 after the codemod.
      - `npm run build` (Next) → exit 0 after the codemod
        (per AC 8).
      - `npm test` (vitest) → exit 0; test count unchanged.
      - `git status --short` (fresh per **L-005**) shows
        only the expected new/modified paths:
        `components/app/Sketch/Sketch.module.css` modified,
        `projects/2026-05-02-agent-guilds/codemod-exercises/`
        + checkin dir untracked.

  12. **D3 own antagonist panel — explicit specialist
      panel via `Panel: +evaluator-css-architecture`**:
      - The target file is a `.module.css` so
        `derive-panel.ts` yields `evaluator-contract-fit +
        evaluator-tokens + evaluator-naming` by default.
      - Per the Phase 4 PANEL-COMPOSITION update, this unit
        names `Panel: +evaluator-css-architecture` to invoke
        the specialist with elevated precedence (this is
        the FIRST time the specialist is in a unit's panel,
        validating the override mechanism).
      - Effective panel for D3: **`evaluator-contract-fit,
        evaluator-css-architecture, evaluator-tokens,
        evaluator-naming`** (specialist slotted by
        precedence). Net 4-evaluator panel.

- **Rules applied** (re-verified at close):
  - `npm run lint` clean.
  - `npm run build` clean (the codemod's new token must
    resolve).
  - `npm test` passes.
  - **L-001** honored: no `tsx` invocation.
  - **L-004** honored: D3 executed in a fresh Claude Code
    process; cited in Notes for the PR.
  - **L-005** honored: fresh `git status --short` before
    evaluator panel.
  - **L-009** honored: dense packet for D3 own-eval.
  - **L-013** honored: claims about generator/evaluator
    behavior in the results doc cite verbatim Agent-tool
    outputs.

- **Disqualifiers**:
  - No edits to agent files (`generator-*`, `evaluator-*`,
    `whiteboard-*`). All Phase 4 territory closed in D1/D2.
  - No edits to `/guild-*` skills, `/ev-loop-*` skills,
    `/trout-*`, `/griot-*`.
  - No edits to scripts (`derive-panel.ts`,
    `parse-and-aggregate.ts`, `whiteboard.ts`).
  - No edits to `PANEL-COMPOSITION.md`, `CONVENTIONS.md`
    (D1/D2 territory).
  - **The codemod is the ONLY product-code change**. No
    edits to `sketches/`, `tokens/`, `styles/`, `app/`, or
    other `components/**/*.module.css` files. The codemod's
    scope is the named file only.
  - No new agent files.
  - No new tests authored.
  - No `tsx` invocation.
  - **No execution in the current Claude Code process**.
    The contract draft is on disk; execution requires fresh
    session.

- **Inputs**:
  - `.claude/agents/generator-css-codemod.md` (from D1) —
    the active generator being exercised. Body documents
    sketch-CSS carve-outs and resolved-value-diff requirement.
  - `.claude/agents/evaluator-css-architecture.md` (from
    D1) — the specialist evaluator paired with the
    generator.
  - `.claude/agents/generator-base.md` (from D1) — shared
    contract for the family.
  - `.claude/agents/PANEL-COMPOSITION.md` (extended in D1)
    — the precedence list + boundary spec that places
    `evaluator-css-architecture` at position 4 with
    elevated specialist precedence.
  - `projects/CONVENTIONS.md` (extended in D2) —
    "Generator-evaluator pair authoring" pattern doc.
  - `components/app/Sketch/Sketch.module.css` (touched in
    D2 to add the load-bearing-patterns comment) — D3's
    target file.
  - `tokens/tokens.ts` / `tokens/design-tokens.json` —
    available tokens the generator examines when choosing
    a token name.
  - `projects/2026-05-02-agent-guilds/PLAN.md` § Phase 4 —
    the verification clause being checked.
  - `projects/2026-05-02-agent-guilds/whiteboards/4-domain-pairs.md`
    — Phase 4 phase-start whiteboard that produced the
    two reframes shaping D1/D2/D3.

## Execution

1. **Branch cut + prelude commit** (`cd860e9`, prior session).
   Standard carryover pattern. Then the prior session committed
   the D3 contract draft (`b6c774b`) and a session handoff
   (`19f999a`) before the L-004 process restart.

2. **Resumed in fresh Claude Code process** (this session). Per
   the L-004 split pattern this is the **third observed
   instance** (after panel-8 D8 for Phase 2 and whiteboard-3 D3
   for Phase 3) — the `generator-css-codemod` and
   `evaluator-css-architecture` agents authored in Phase 4 D1
   (PR #71, merged) were on disk but not in the prior
   session's process registry. The fresh session loaded them
   into the agent registry at process start; both spawned
   successfully in this session.

3. **Pre-flight verified**: `npm run lint` exit 0,
   `npm run build` exit 0, `git status --short` clean (post-
   fetch, post-prelude). Tokens namespace confirmed:
   `tokens/tokens.ts:94` declares `bg.inverted.default`;
   `styles/tokens.css:67` (light) and `:113` (dark) define the
   resolved values.

4. **Generator spawned** via `Agent` tool with
   `subagent_type: generator-css-codemod`. Brief named
   `Sketch.module.css` as target, `background-color: black;`
   as the migration source, `bg.inverted.default` as the
   proposed target token (verified-to-exist), and listed the
   three carve-outs to preserve (`!important` on canvas
   width/height; `& canvas` descendant; D2 comment block).

5. **Generator output**: one Edit operation on line 17.
   `background-color: black;` →
   `background-color: token("bg.inverted.default");`. All
   carve-outs preserved verbatim. Generator produced a
   resolved-value diff table per its base's output contract.

6. **Generator surfaced a substantive concern** in its
   narrative — the resolved value INVERTS between color
   schemes (`hsl(0deg 0% 8%)` light → `hsl(0deg 0% 92%)`
   dark). The literal was scheme-invariant black. This is
   not a silent value change (the generator was loud) but
   the design implication is real. The generator framed it
   as two interpretations (page-chrome-relative vs absolute-
   stage) and deferred the call to the orchestrator/user.

7. **Generator tool-denial flagged**: the brief asked for one
   `npm run build` to verify the token resolves; the
   generator's tools allowlist excludes Bash. The generator
   flagged this in its narrative, verified token existence
   by direct file lookup instead, and deferred build to the
   orchestrator. The loop ran `npm run build` → exit 0.
   **The generator-base.md "no Bash" allowlist is correct by
   design** — generators propose, orchestrator runs
   verification + commits. Contract-language refinement
   captured as a substrate finding in the results doc.

8. **Specialist evaluator spawned** via `Agent` tool with
   `subagent_type: evaluator-css-architecture`. Packet
   included the post-codemod artifact, the diff table, the
   generator's flagged concern, and the build-verification
   result. **First real invocation of the specialist
   evaluator** since its authoring in D1.

9. **Specialist verdict**: PASS with one advisory finding.
   Carve-outs preserved; diff present; scope respected
   (specialist explicitly noted `FormulaSketch.module.css:2`
   has the same `black` literal but was correctly NOT touched
   per D3's per-file scope); structural shape clean.
   Specialist minted a new advisory code on first invocation:
   **`css-arch-semantic-inversion-flagged`** (the codemod
   surfaced a resolved-value inversion; not silent; awaits
   human design decision). Specialist also articulated the
   **partnership-shape precedent** explicitly — "when the
   generator surfaces a substantive concern, the specialist
   acknowledges, doesn't escalate to blocking; blocking is
   reserved for unflagged drift, contract violations, or
   carve-out regressions."

10. **User decision via AskUserQuestion** per **L-002**:
    surfaced the canvas-stage semantic question as a three-
    way fork (accept inversion / revert / expand to author
    a new token). User chose **accept the inversion** —
    the codemod ships as-is. Documented precedent path for
    the FormulaSketch sibling in a future unit.

11. **Results doc authored** at
    `projects/2026-05-02-agent-guilds/codemod-exercises/4-sketch-module-css.md`.
    Captures both invocations, full diff table, specialist
    verdict, Phase 4 verification cross-reference, and four
    substrate findings surfaced by the exercise.

12. **D3's own panel**. Per AC 12, the panel auto-derives to
    `evaluator-contract-fit + evaluator-tokens + evaluator-
    naming` for the .module.css + .md file set. The
    contract explicitly invokes `evaluator-css-architecture`
    via `Panel: +evaluator-css-architecture` override — this
    is the **first time the specialist is in a unit's own
    panel** (validating the override mechanism end-to-end).
    Effective panel: 4 evaluators.

## Scope

- **Created**:
  - `projects/2026-05-02-agent-guilds/codemod-exercises/4-sketch-module-css.md`
    — the D3 results doc.
  - `projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.domain-pairs-3/01.md`
    (this checkin).

- **Modified**:
  - `components/app/Sketch/Sketch.module.css` — line 17
    only: `background-color: black;` →
    `background-color: token("bg.inverted.default");`. CSS
    rules otherwise unchanged; carve-outs preserved.

- **Modified in prelude commit `cd860e9`**:
  - `projects/2026-05-02-agent-guilds/MANIFEST.md` — phase-4
    `#73 (open) → merged` reconcile + branch-cut event row.

- **Not touched** (per disqualifiers):
  - No agent file edits (Phase 4 D1 territory).
  - No `/guild-*` / `/ev-loop-*` / `/trout-*` / `/griot-*` /
    script edits.
  - No `PANEL-COMPOSITION.md` / `CONVENTIONS.md` edits.
  - **Product code touched ONLY in Sketch.module.css**.
    `FormulaSketch.module.css:2` (sibling case) NOT touched
    per scope discipline.
  - No `tokens/` edits; no `npm run generate:tokens` run.
  - No new agent files; no new tests; no tsx.

## Pre-computed verification (authoritative)

- `npm run lint` (Biome) → exit 0; "Checked 129 files in
  27ms. Found 2 warnings" (pre-existing, unchanged).
- `npm run build` (Next) → exit 0 BOTH before and after the
  codemod. The `token("bg.inverted.default")` PostCSS call
  resolves correctly via the project's token pipeline.
- `npm test` (vitest) → not re-run; 18/322 baseline stands.
- `git status --short` (fresh per **L-005**) → 1 modified
  (`Sketch.module.css`) + 1 untracked dir
  (`codemod-exercises/`) + 1 untracked dir (this checkin).
  MANIFEST committed in prelude.
- `derive-panel.ts` on D3's artifact list →
  `evaluator-contract-fit,evaluator-tokens,evaluator-naming`.
  Specialist added via explicit `Panel: +
  evaluator-css-architecture` override per AC 12.

## Evaluator verdict

**Panel**: `evaluator-contract-fit, evaluator-css-architecture,
evaluator-tokens, evaluator-naming` — 4 evaluators total,
**first invocation of the specialist via `Panel: +` override**.
The specialist's elevated precedence per PANEL-COMPOSITION
applies if any overlap-resolution is needed; the panel runs in
parallel.

**Run 1 of 1**: `approved` from all four evaluators.
- `evaluator-contract-fit`: 12 ACs verified; soft note that
  AC 9 (tokens-no-longer-flagging) and AC 10 (L-004
  labeling) are inferable from the artifact but could be
  more explicitly labeled next iteration.
- `evaluator-css-architecture` (via `Panel: +` override, first
  use): prior advisory faithfully captured in results doc;
  structural outcome unchanged from execution-time PASS.
- `evaluator-tokens`: the migrated line is no longer a
  `tokens-named-color` flag site; codemod's whole purpose
  satisfied.
- `evaluator-naming`: `bg.inverted.default` names the meaning
  (page-chrome-inverted backdrop) under the user's "accept
  the inversion" framing; no regression.

**No disqualifier fired.**

## Notes for the PR

- **L-004 third observed instance**. This unit required a
  Claude Code process restart between contract drafting
  (prior session, ending with PR #73 merge) and execution
  (this session). The `generator-css-codemod` and
  `evaluator-css-architecture` agents authored in PR #71
  (D1 of Phase 4) were on disk after merge but not in the
  prior session's process registry. The pattern is now
  load-bearing substrate behavior — three observed
  instances:
  - **panel-8 D8** (Phase 2 multi-evaluator smoke): five
    Phase 2 evaluators authored across PRs #44-#49 not
    callable until process restart.
  - **whiteboard-3 D3** (Phase 3 design exercise): six
    whiteboard engineer agents from PR #58 not callable
    until restart.
  - **domain-pairs-3 D3** (this unit): `generator-css-codemod`
    and `evaluator-css-architecture` from PR #71 not
    callable until restart.

  L-004 should be sharpened in the next `/griot-compact`
  cycle to call out "process restart, not `/clear`"
  explicitly — and `generator-base.md`, `evaluator-base.md`,
  `whiteboard-base.md` should reference the pattern in body
  prose for future agent authors. Both are follow-up units,
  not D3 territory.

- **The substrate worked as designed for the first time
  with a real codemod**. The generator surfaced a
  substantive semantic concern (dark-mode resolved-value
  inversion) in its narrative — it was LOUD about a
  not-visually-neutral migration. The specialist evaluator
  acknowledged the concern, refused to escalate to blocking
  (the generator's loudness satisfied the
  `css-arch-silent-value-change` trigger condition), minted
  a new advisory code (`css-arch-semantic-inversion-
  flagged`), and articulated the partnership precedent:
  blocking is for unflagged drift; flagged-with-narrative
  belongs in advisory. This is the substrate's first
  articulation of the generator-specialist working pattern
  in practice. Worth elevating to the agent body prose in
  a follow-up.

- **correction: user adopted "accept the inversion" via
  AskUserQuestion**. The dark-mode flip is real
  (`hsl(0deg 0% 8%)` light → `hsl(0deg 0% 92%)` dark).
  Under the page-chrome-relative framing the user adopted,
  this is the semantically-correct outcome — the canvas
  contrasts with the surrounding page in both schemes.
  Generative sketches that draw with theme-aware logic
  adapt; those that assume an absolute-dark stage need
  to either be updated or have a future token authoring
  intervention. The user's "accept the inversion" decision
  is documented; the FormulaSketch sibling will follow the
  same path in a future unit (codemod precedent set).

- **The Phase 4 phase-start whiteboard's substrate value
  paid out twice**:
  1. **In D1**, where the skeptic flipped the specialist-
     solo-first sequencing into specialist-elevated-precedence-
     in-parallel-panel before any code shipped.
  2. **In D3 (this unit)**, where the generator's
     resolved-value diff table (a contract requirement that
     came from a11y's round-1 contribution to the Phase 4
     whiteboard) surfaced the dark-mode inversion. Without
     the diff table, the migration would have shipped
     silently and broken sketches authored against absolute-
     dark backdrops. With the diff table, the inversion was
     a deliberate user decision.

  Both moments demonstrate the always-on-whiteboard-step
  paying for itself: pre-design conversation surfacing
  concerns BEFORE code locks them in.

- **First use of `Panel: +<specialist>` override mechanism**:
  D3's own panel includes `evaluator-css-architecture` via
  explicit override (not auto-derivation). The mechanism
  was added in D1's PANEL-COMPOSITION extension; this is
  its first real exercise. Worked exactly as designed —
  the specialist participated in the panel with elevated
  precedence per spec.

- **Substrate findings worth elevating to next
  `/griot-compact`** (in addition to the L-004
  strengthening above):
  1. **Specialist-minted advisory code**
     `css-arch-semantic-inversion-flagged` — should be
     added to `evaluator-css-architecture.md`'s 10-entry
     rubric so future invocations have it canonically.
  2. **Generator-specialist partnership precedent** — the
     specialist's articulation ("acknowledge, don't
     escalate, when the generator was loud") is the first
     written substrate guidance on the pattern. Worth
     promoting into agent base bodies.
  3. **Generator verification-budget contract language** —
     the brief asked the generator to run `npm run build`,
     but the generator's tools allowlist excludes Bash. The
     "no Bash" allowlist is correct by design (generators
     propose, orchestrator runs verification + commits) but
     contract language should address-the-loop ("the loop
     may run...") rather than address-the-generator ("you
     may run...") so the next contract-writer doesn't
     repeat the framing.
  4. **The Sibling case for future codemod work**:
     `components/app/FormulaSketch/FormulaSketch.module.css:2`
     has the same `black` literal. A future codemod unit
     can batch with this — same decision, two files.

- **L-001 honored**: no `tsx` invocation.
- **L-002 honored**: surfaced the dark-mode-inversion fork
  to the user via AskUserQuestion before committing the
  codemod; the user adopted "accept the inversion." Pure
  L-002 example.
- **L-005 honored**: fresh `git status --short` before
  evaluator panel.
- **L-009 honored**: dense packet for D3 own-eval (with
  the unusual feature of being a 4-evaluator panel where
  one is the specialist evaluating its prior work; the
  packet explicitly notes the prior verdict is captured
  in the results doc so no re-evaluation needed).
- **L-013 honored**: every prose claim about
  generator/evaluator behavior cites verbatim Agent-tool
  outputs (captured in the results doc).

- **Phase 4 closes** with this PR's merge. All three
  Phase 4 deliverables shipped:
  - D1 (#71): generator-* family + active pair +
    PANEL-COMPOSITION extension.
  - D2 (#73): CONVENTIONS pair-authoring pattern +
    Sketch.module.css load-bearing-patterns comment.
  - D3 (this PR): real CSS codemod exercise end-to-end.
  Phase 5 (Griot integration + composability proof) is
  next-eligible after this PR merges.
