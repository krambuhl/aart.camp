# Session 2026-05-10-a

**Phases touched**: 1.5
**Checkins written**: 12, 13, 14
**PR activity**: #30 merged (D10), #32 merged (D11), #33 opened (D12, awaiting merge)

## What happened

Closing arc for Phase 1.5. Shipped the final three deliverables back-to-back: D10 (`trout-plan` scaffold split → `plan-scaffold.ts`), D11 (`griot-use` skill deletion + script + ev-run inline), and D12 (e2e verification of the migrated substrate + cleanup of the deferred-from-D10 `autosave.ts --init` path). Phase 1.5 is now marked complete in the MANIFEST; PR #33 carries the closing checkin and is awaiting merge.

Three structurally different shapes shipped in sequence: D10 was a standard LLM/CRUD split (matching D6-D9), D11 was a skill-deletion + callsite-update + permissions-cleanup combo (no LLM body to preserve since `griot-use` was already pure-CRUD), and D12 was a meta-deliverable that exercised every migrated script against a throwaway test project (now archived at `projects/archive/2026-05-09-phase-1-5-test/`) plus the `--init` cleanup. All three approved on first evaluator run.

The D12 e2e exercise surfaced two substrate findings on `archive-relocate.ts` (logged in checkin 14 Notes, not addressed in-line per the no-co-located-refinements discipline). One mid-flight contract correction on D10 (SKILL line cap was over-aggressive; renegotiated from ~120 to ~155 since the LLM-shaped interview prose is intentionally untouched). One mid-flight test-caught-my-own-comment moment on D11 (the tier-separation invariant test correctly flagged my explanatory comment naming the forbidden subdirectories — reworded, all green).

## Open threads

- **PR #33 awaits merge.** Closes Phase 1.5. After merge, `agent-guilds` next session should run `/ev-run agent-guilds` and the autoload briefing will surface Phase 2 as the next eligible phase. (Phases 2, 3, 4 can land in any order per dependency table; suggest starting with Phase 2 — antagonist evaluator panel — since it unblocks Phase 5.)
- **Two new substrate findings on `archive-relocate.ts`** (D12 surfaced):
  - Requires git-tracked source (uses `git mv`); first archive attempt failed with `fatal: source directory is empty`. Real workflow naturally satisfies this since each checkin commits before next operation, but the requirement isn't documented. Tracked for tiny doc-sweep follow-up.
  - Modifies the manifest *after* `git mv`, leaving the Status flip unstaged. Required a separate "Flip Status" commit. Substrate-refinement candidate: modify-then-mv would atomicize.
- **Refinement backlog (carried from prior sessions, still untouched):** five session-b items (autoload-gh reconciliation, commit-allowlist tightening, submit pr-number sanity check, autosave-phase-update Current-branch propagation, submit-N-placeholder substitution); CONVENTIONS.md line 65 `node:test` text vs. vitest reality; `resolveProject` four-times-duplicated across the trout family (D11 didn't add to the count since `griot/use.ts` doesn't need it); `Item.location` overload from D9. All eligible for tiny standalone follow-up PRs after Phase 1.5 lands.
- **Phase 2 prep**: PLAN.md § Phase 2 specifies `evaluator-a11y` + `evaluator-nextjs` (full antipattern catalogs + CLI validators), plus `evaluator-react-api`, `evaluator-tokens`, `evaluator-naming` (smaller advisory-only catalogs initially). Next session can start with planning the unit breakdown — likely 3-5 deliverables (one per evaluator family with its catalog).
- **Harness session-cache quirk noted in D11**: `/griot-use` skill was deleted on disk this session but the harness's session-cached skill list still surfaced it for the rest of this session (per the agent-registry session-cache pattern documented in user memory). New sessions will not see it.
- **Sibling project `griot-subagents` shipped #31 in parallel** (regression-and-pr-body work). No conflicts with `agent-guilds`. Worth syncing main again before cutting cleanup-N+1 in any future session.

## Notes

- **Pattern observation: contract caps got better calibrated as the arc went on.** D10 had a contract-cap miscalibration (~120 SKILL line cap was too tight; renegotiated mid-flight to ~155). D11 came in well under all caps. D12 came in well under all caps including a much-better-than-estimated `--init` cleanup (-114 lines vs. the ~70 estimated; runInit's inline manifest template was longer than I'd remembered). For Phase 2 contract drafting: derive script line caps from the closest-shape precedent script's actual size, not a round number.
- **D11 lesson worth carrying forward**: when a test enforces a textual invariant on the source, comments count as source. The tier-separation test correctly flagged my own explanatory comment naming `session-notes` and `nightly`. Future invariant-on-source tests should think about whether documentation prose should be excluded (and how — my D11 fix excluded the citation-contract literal but not arbitrary comments).
- **Two-step autosave-after-submit is still ugly** (carried from prior sessions, unresolved). Each new-PR submit produces TWO tracking commits — one for `pr-opened`, one for the post-submit `phase-row PR field updated` note. Three PRs this session × 2 tracking commits each = 6 noise commits. The session-b refinement #5 (`submit <N>` placeholder substitution) would collapse both into one. Worth doing as a tiny maintenance PR before Phase 2 starts opening more PRs.
- **No corrections recorded in checkin 14's "Notes for the PR" with `correction:` prefix.** The two substrate findings on `archive-relocate.ts` are flagged as "Substrate finding" not "correction:" — they're discoveries about the substrate's existing behavior, not corrections to my work that I want griot-capture to ingest. Checkins 12 and 13 do have `correction:` lines (D10's SKILL line cap renegotiation; D11's tier-separation comment fix) — those are valid griot-capture targets.
