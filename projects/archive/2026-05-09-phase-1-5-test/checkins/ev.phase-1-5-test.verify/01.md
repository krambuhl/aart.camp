# Checkin 01 — ev.phase-1-5-test.verify

**Created**: 2026-05-09 22:15
**Phase**: 1 — Verify
**Unit**: Document Phase 1.5 substrate end-to-end verification exercise

## Contract

**Goal**: Document that the migrated Phase 1.5 substrate scripts (`plan-scaffold.ts`, `autoload.ts`, `save-session-finalize.ts`, `archive-relocate.ts`) compose correctly into a real project lifecycle. This checkin is the test artifact; the parent project's checkin 14 (on `agent-guilds`) holds the verification log.

**Acceptance**: All four migrated scripts run without error against this test project. Each produces its expected artifact:
- `plan-scaffold.ts` → this project directory exists with `PLAN.md`, `config.md`, `MANIFEST.md`, `sessions/`, `checkins/`.
- `autoload.ts` → briefing renders with title, status, phase row, "no work started yet" state.
- `save-session-finalize.ts` → session note appears at `sessions/2026-05-09-a.md`.
- `archive-relocate.ts` → this entire directory relocates to `projects/archive/2026-05-09-phase-1-5-test/` with `Status` flipped to `archived`.

## Execution

This checkin is the test artifact. The verification log lives in
`projects/2026-05-02-agent-guilds/checkins/ev.agent-guilds.phase-1-5-substrate-cleanup-9/14.md` (the D12 checkin on the parent project), which records each script invocation and its output.

## Evaluator verdict

N/A — verification is mechanical. The substrate's evaluator path (`evaluator-contract-fit` via `/guild-validate`) was already exercised by the parent project's checkins 12 and 13 in this same arc; running it for a contrived dummy unit adds no signal. The parent checkin 14's pre-computed verification (script outputs, artifact presence, MANIFEST state) IS the evidence.

## Notes

- This project is throwaway. It exists solely to provide a real `projects/<slug>/` target for the migrated scripts to operate on. After D12 ships, this directory lives at `projects/archive/2026-05-09-phase-1-5-test/` as the archived artifact of the verification exercise.
