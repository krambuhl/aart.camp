[
  {
    "id": "L-001",
    "title": "Node 24 strips TS — use `node` directly",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-06T10-22-36-node-24-strips-types",
    "body": "When authoring a TypeScript script for direct invocation under Node 24+,\nuse `node script.ts` directly — Node 24 strips type annotations natively\n(the flag was experimental in 22.6, default-on in 23, fully default in\n24). Do not propose `tsx`, `npx tsx`, or any other transformer chain for\nthis case. The substrate runs faster, has one fewer devDep on the\ncritical path, and the invocation form is shorter (`Bash(node ...)` vs\n`Bash(npx tsx ...)`).\n\nCaveat: the source must use only \"erasable\" TypeScript — no enums, no\n`namespace`, no parameter properties, no `const enum`. Stick to type\nannotations on values, interfaces, type aliases, and `as const`. If\nbroader TS features are required, then tsx becomes necessary again, but\nthat's the unusual case for substrate plumbing.",
    "rubric": [
      "Output invokes TypeScript scripts using `node` directly (e.g., `node script.ts`) rather than via `tsx` or `npx tsx`",
      "Output does not contain `npx tsx` or `tsx` as a runtime wrapper for executing `.ts` substrate scripts",
      "Output does not propose adding `tsx` as a dependency, permission pattern, or invocation convention for running `.ts` scripts"
    ]
  },
  {
    "id": "L-002",
    "title": "Halt-and-fork on mid-flight scope expansion",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T14-29-27-mid-flight-scope-fork-pattern",
    "body": "When you honor a contract's literal \"no X changes\" disqualifier but\ndiscover during validation that adjacent work (test files, harness\nconfig, dependency-graph-adjacent edits) was implicitly excluded by the\nframing, halt and raise it as a fork to the user before proceeding.\nDon't silently expand scope, and don't make a single recommendation —\npresent multiple named options (typically a defer / do-here / new-checkin\nor precursor-commit shape) so the user can pick the path that matches\ntheir priorities. The boundary-expansion conversation belongs in the\nsession log, not in the diff.",
    "rubric": [
      "Output halts and waits for user input rather than proceeding to make the out-of-scope changes (e.g. porting the test files) in the same checkin",
      "Output explicitly names that the discovered work falls outside the original contract's scope or boundary",
      "Output presents the user with a fork of at least two named options (e.g. defer / do here / new checkin) for how to handle the scope expansion"
    ]
  },
  {
    "id": "L-003",
    "title": "Responsive Stack direction needs responsive gap",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-04-30T20-30-50-responsive-gap-hierarchy",
    "body": "When a `Stack`'s `direction` is responsive (e.g., `{ xs: 'vertical', sm: 'horizontal' }`), its `gap` value is interpreted on different *axes* across breakpoints. A fixed gap that reads as comfortable horizontal breathing room becomes a vertical gap on the smaller breakpoint, where it can compete with the outer container's gap on the same axis. If the inner gap is larger than the outer container's gap on `xs`, items inside each row will visually pair with the wrong neighbor (e.g., a date sitting closer to the next title than to its own).\n\nWhen a Stack changes direction across breakpoints, make `gap` responsive too. Pick the inner gap on `xs` to be visibly smaller than the outer container's `xs` gap so nested pairs read as a unit. Keep the larger `gap` value on `sm+` where it lives on the horizontal axis and doesn't compete with vertical row spacing.",
    "rubric": [
      "The inner row Stack's `gap` prop is a responsive object (e.g. `{ xs: ..., sm: ... }`) rather than a single fixed token value like `tokens.space.x16`",
      "On the `xs` breakpoint, the inner row Stack's gap resolves to a value strictly smaller than the outer column gap of `tokens.space.x6` (so the date visually pairs with its own title, not the next one)",
      "On the `sm` breakpoint (or larger), the inner row Stack's gap remains a comfortable horizontal spacing value (e.g. `tokens.space.x16` or similar), not collapsed to the tighter `xs` value"
    ]
  },
  {
    "id": "L-004",
    "title": "Split agent-creation from agent-use at session boundary",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-04T03-08-00-agent-registry-session-cache",
    "body": "Claude Code loads the `.claude/agents/` registry at session start. Agent files added during a session — new `evaluator-*.md`, `generator-*.md`, or `whiteboard-*.md` — are not callable as `subagent_type` until the session restarts. The file exists on disk and is structurally correct; the runtime registry is just session-cached.\n\nWhen authoring a unit that introduces a new agent file AND uses that agent (smoke test, runtime verification, replacement of an existing agent), split the unit at the session boundary:\n\n- **Unit N** (current session): create the agent file. Optionally use the existing/bridge agent for whatever work needs an agent in this session.\n- **Unit N+1** (next session, first action): the registry has reloaded; spawn the new agent as a smoke test, then proceed with whatever depends on it (deletion of bridge file, swapping the loop's spawn target, etc.).\n\nDo NOT delete a bridge file in the same session that introduces its replacement. The bridge keeps the existing path functional through the rest of the current session and gives the next session a known-good fallback.\n\nConcrete pattern: when phase 2-4 of the agent-guilds project introduces new evaluators (`evaluator-a11y`, `evaluator-tokens`, etc.), generators (`generator-css-codemod`, etc.), or whiteboard engineers, contracts that author these agents should NOT also bundle \"use this agent\" steps. Use-steps are a follow-up unit's responsibility.",
    "rubric": [
      "Output separates the authoring of new agent files from any step that invokes those agents at runtime, placing the invocation step in a distinct follow-up unit rather than the same unit as the file creation",
      "Output explicitly notes that the follow-up unit invoking the new agent must run in a fresh/new session (referencing session boundaries or registry reload at session start)",
      "Output does not propose working around the registry constraint by faking verification in prose or by skipping/deleting the existing fallback before the new agent is runtime-verified"
    ]
  },
  {
    "id": "L-005",
    "title": "Re-run git status fresh before evaluator for state-shape criteria",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-06T04-19-40-pre-eval-fresh-git-status",
    "body": "When a unit's acceptance criterion specifies tree state at exact granularity — \"`git status` shows only X and Y\" or any binary state-shape claim — re-run the underlying command immediately before invoking the evaluator and record its output verbatim into the checkin. Do not record the verification claim from a recalled summary earlier in the session, even if the unit's own work hasn't touched anything outside the named files. Framework auto-generation (Next.js writing to `next-env.d.ts`, build-output references, codegen on dev/build hooks) can silently mutate tracked files between the time you \"knew the state\" and the time the evaluator reads it.\n\nConcrete pattern: every binary state-shape acceptance criterion gets a paired pre-evaluation step. `git status` → record output → check against criterion → invoke evaluator. Failing the check shifts the unit into a fix-or-amend-contract decision before the evaluator round, not after. Recalled summaries are evidence about *intent*, not state.",
    "rubric": [
      "The output shows evidence that `git status` was run fresh as part of the verification step that records the working-tree state (e.g., a tool invocation, command transcript, or explicit statement of a just-run check), rather than citing a recalled or earlier-in-session state",
      "The enumerated files in the recorded `git status` claim include every path that a fresh `git status` would surface at that moment (no silent omissions of auto-generated or framework-managed files like `next-env.d.ts`)",
      "The output does not assert a working-tree state criterion (`git status shows only X and Y`) on the basis of an earlier-session summary or an unverified assumption that nothing has changed since"
    ]
  },
  {
    "id": "L-006",
    "title": "Parse markdown vocabularies at runtime, no TS-const + drift-test",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-06T10-23-57-parse-conventions-at-runtime",
    "body": "When a substrate script needs a closed vocabulary or schema that's already documented in a project markdown file (e.g. `CONVENTIONS.md`), parse the markdown at runtime. Do not propose a shared TypeScript const file plus a drift-detection test as the recommended approach — that introduces three artifacts (const, MD, drift test), three places to update on every vocabulary change, and a whole class of synchronization bugs that disappear if the script just reads the source of truth directly.\n\nParsing a small markdown table is sub-millisecond (single-digit microseconds per parse for files under a few hundred lines), so performance is not a concern for the kind of substrate work this applies to. Provide a hardcoded fallback only as a defensive measure for the unreadable-file case, with a comment noting the file is the authority. Resist the instinct to \"type-safe everything by extracting to a const\" — when there's already one source of truth in human-readable form, keep it that way.",
    "rubric": [
      "Output recommends parsing `projects/CONVENTIONS.md` at runtime as the source of vocabulary, rather than maintaining a TypeScript constant that duplicates the vocabulary",
      "Output does not propose a drift test, sync test, or set-equality assertion between a TS constant and CONVENTIONS.md",
      "Output treats CONVENTIONS.md as the single source of truth for the shared vocabulary (no parallel TS const definition of the same vocabulary values)"
    ]
  },
  {
    "id": "L-007",
    "title": "Consolidate overlapping tooling at rebase, don't coexist",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-06T22-55-01-rebase-scope-expansion",
    "body": "When a rebase brings in tooling that overlaps with what the current PR is standing up — competing test runners, competing linters, competing build pipelines — the right resolution is consolidation, not coexistence. Two overlapping tools is permanent maintenance burden (two configs to drift, two CI invocations, two assertion styles for contributors to learn); one tool with a focused filter script (e.g., `test:agentic` to run just the substrate slice under vitest) is the cleaner abstraction. Default to porting the smaller surface onto the tool the PR already adopts, especially when the port is mechanical (a few-line edit at most).\n\nThis is still infra work and stays in scope for an \"infra not coverage\" PR — one harness instead of two is an infrastructure simplification, not coverage expansion. The instinct to defer consolidation as a separate follow-up PR (\"preserve scope by not touching the conflicting code\") leaves the project worse off than the rebase found it. Surface the scope expansion clearly to the user (write a `correction:` line in the checkin Notes per substrate convention), confirm the resolution, then do the consolidation in the same PR.",
    "rubric": [
      "Output recommends porting/converting the substrate tests to vitest (one harness) rather than running node:test and vitest side by side",
      "Output does not propose a separate `node --test` script (e.g., `test:substrate` invoking `node --test`) as the final state",
      "Output does not defer harness consolidation to a follow-up PR"
    ]
  },
  {
    "id": "L-008",
    "title": "Parser leniency for non-canonical header forms",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-07T15-30-25-notes-header-leniency",
    "body": "When a checkin's Notes section uses a non-canonical header form (`## Notes for PR` vs CONVENTIONS.md's `## Notes for the PR`), the parser must accept both rather than failing silently. Self-smoke against the real project caught this — the script was producing a Last checkin block without the Notes line because the regex looked for the canonical form only. Worth knowing for the next migration: don't assume canonical-form headers in fixtures match what's actually on disk.",
    "rubric": [
      "Output's parser regex for the Notes header matches both `## Notes for PR` and `## Notes for the PR` (e.g., uses an optional 'the ' group such as `(?:the )?`)",
      "Output includes or adds a test case that exercises the non-canonical `## Notes for PR` (without 'the') header form against the parser",
      "Output does not restrict the Notes header match to only the canonical `## Notes for the PR` form"
    ]
  },
  {
    "id": "L-009",
    "title": "Dense-packet pattern — pre-compute and instruct trust",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T14-09-52-evaluator-trust-precomputed",
    "body": "Evaluator agents work better when the packet trusts them with pre-computed verification rather than asking them to re-investigate. The original wording (verbatim contract + raw artifact pointers) implicitly invites tool-heavy investigation; the dense-packet wording explicitly says \"trust the pre-computed results and spot-check at most one or two criteria.\" Same evaluator agent, same maxTurns budget — packet shape is the variable.",
    "rubric": [
      "Output does not instruct passing the Contract section verbatim (or the original ask verbatim) into the evaluator packet",
      "Output describes the packet as containing pre-computed verification results or a pre-computed summary of criterion outcomes rather than raw artifact pointers alone",
      "Output explicitly tells the evaluator to trust the pre-computed results and limit independent investigation to at most one or two spot-checks"
    ]
  },
  {
    "id": "L-010",
    "title": "Stage-path filter: deny-list, not allowlist",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T14-09-52-stage-paths-allow-untracked-code",
    "body": "The staging logic for new code files outside `projects/` was initially too restrictive (only stageable if matching the checkin prefix), which would have rejected new substrate files like `.claude/scripts/trout/pr-plumbing.ts` itself. Updated `selectStagePaths` to permit untracked files outside `projects/` (filtered by the hard exclude list) — modeled on the recent commit history showing new substrate files getting committed alongside checkins.",
    "rubric": [
      "Output's staging logic permits untracked files outside the `projects/` directory (does not reject them solely for being outside `projects/`)",
      "Output filters stageable paths using a hard exclude list rather than restricting non-`projects/` files to a checkin-prefix or `NN.md` pattern allowlist",
      "Output does not require untracked files outside `projects/` to match the checkin prefix (e.g., `NN.md`) in order to be staged"
    ]
  },
  {
    "id": "L-011",
    "title": "Text-based selection, not index-based, for human-edited lists",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T14-10-09-text-based-selection-not-index",
    "body": "Index-based selection in scripts that pick from a list (e.g. `--correction-index=N`) is fragile when the list can be reordered. Text-based selection with whitespace-tolerant match is the safer default — if the source list changes, the script either still finds the text or fails loudly with the available options. Apply this pattern to similar situations in the substrate (e.g. picking a checkin from a branch).",
    "rubric": [
      "Output does not use an integer-index argument (e.g. `--correction-index=<N>` or equivalent positional integer) to select which correction to operate on",
      "Output selects the correction by matching against the correction's text content (e.g. a `--correction-text=` / `--match=` style argument or equivalent string-based lookup)",
      "When the text-based selection finds no match, the script fails loudly and surfaces the available correction options rather than silently proceeding or defaulting"
    ]
  },
  {
    "id": "L-012",
    "title": "Surface recurring cross-project patterns, fix upstream",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T14-29-06-agent-guilds-node-test-recurrence",
    "body": "agent-guilds project (the `2026-05-02-agent-guilds` peer project) keeps shipping substrate tests written against `node:test` rather than `vitest`. Three more landed via main's fast-forward into Phase 4's branch. Phase 1 D3 already established the porting pattern for the original autosave test, but the upstream project doesn't yet know to author tests against vitest directly. Right fix is upstream — either a CLAUDE.md note in the agent-guilds project's scope or a lint rule that flags `import { test } from 'node:test'`. For now, ports happen reactively when this project's CI exposes them. Worth surfacing during the adopt-test-harnesses retrospective so it doesn't keep surprising future phases.",
    "rubric": [
      "Output explicitly identifies the `node:test`-vs-vitest issue as a recurring pattern rather than treating the ports as isolated one-off fixes",
      "Output proposes at least one upstream/structural prevention mechanism (e.g., a CLAUDE.md note scoped to the agent-guilds project, or a lint rule flagging `import { test } from 'node:test'`)",
      "Output references surfacing this in a retrospective or future-phase planning artifact (e.g., adopt-test-harnesses retro) so the pattern is captured beyond the immediate port"
    ]
  },
  {
    "id": "L-013",
    "title": "Ground script-behavior prose in execution, not source-reading",
    "classification": "L",
    "promoted": "2026-05-11",
    "origin": "2026-05-08T16-02-34-dogfood-catches-prose-bugs",
    "body": "Before writing prose that describes what a script does at runtime — what it resolves, substitutes, parses, or passes along — you must ground the description in execution evidence, not in code-reading inference or the shape of the surrounding contract. Run the script against a real input, capture its actual output, and cite that output (a command invocation and the relevant lines of its result) as the basis for the claim. Reading the script's source, its types, or its CLI surface and then narrating what it \"should\" do is the failure mode; the runtime behavior frequently diverges from what the code structure suggests, and prose written from inference will confidently assert substitutions and resolutions that never happen. If you cannot run the script in the current session, do not write a runtime claim as fact — either ask for a dogfood run, or explicitly mark the description as unverified inference from source so a reviewer knows to confirm it.\n\nConcretely: when a CLI like `pr-plumbing.ts submit --phase-update='<N>:in-progress:pr=#<N> (open)'` is involved, do not assume the script resolves `<N>` from the `gh` response and substitutes it before invoking autosave — it does not, and the literal placeholder text ends up in the manifest. The actual pattern, confirmed by running it, is two steps: invoke `submit` without `--phase-update`, parse the new PR number from `submit`'s terminal-state output line, then run a follow-up `autosave --phase-update` with the resolved number to set the phase row's PR field. Treat any prose claim about cross-step value resolution, placeholder substitution, or implicit data flow between subcommands as requiring an execution receipt before it ships.",
    "rubric": [
      "Output does not claim (as fact) that the script resolves or substitutes `<N>` (from a gh response or otherwise) before invoking autosave",
      "Output describes a two-step pattern: invoking `submit` without `--phase-update` first, then parsing the PR number from `submit`'s output and running a follow-up `autosave --phase-update` with that number",
      "Output's description of script behavior is framed as observed/dogfooded runtime behavior rather than inferred from reading the script's source"
    ]
  }
]
