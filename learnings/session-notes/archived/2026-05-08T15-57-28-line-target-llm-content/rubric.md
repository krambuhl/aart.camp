# Rubric

_Immutable. Written by griot-rubric-author with fresh context. Any
attempt to modify this file is a hard violation._

- Output either omits a line-count target for the SKILL.md contract or sets one that accounts for irreducible LLM-shaped body content (templates, verbatim prompts, reference shapes) rather than predicting a shrinkage based solely on what can be delegated to a script
- Output frames structural cleanup or LLM/CRUD split as the primary success criterion, not a line-count target
- Output acknowledges that LLM-shaped content in the skill body establishes a floor on line count that aggressive compression cannot break through
