# Rubric

_Immutable. Written by griot-rubric-author with fresh context. Any
attempt to modify this file is a hard violation._

- Output does not propose installing or using `@storybook/test-runner` as the VRT harness
- Output proposes vanilla Playwright (e.g., `@playwright/test`) as the VRT harness
- Output enumerates stories by iterating `storybook-static/index.json` (or equivalent static index) rather than relying on a Storybook-aware runner
