correction: Mid-PR scope expansion — main moved while we were working on Phase 1 and introduced its own `node:test`-based substrate test harness. Rather than running two test harnesses indefinitely, this PR now also consolidates the substrate tests under vitest (one harness for both surfaces). `npm test` runs both; `test:agentic` filters to just the substrate. This is still infra work (one harness instead of two), not coverage expansion.
