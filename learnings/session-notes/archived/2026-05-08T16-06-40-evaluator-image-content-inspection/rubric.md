# Rubric

_Immutable. Written by griot-rubric-author with fresh context. Any
attempt to modify this file is a hard violation._

- Output explicitly recommends or performs visual inspection of baseline screenshot content (not just byte-level/hash determinism) before accepting a VRT baseline
- Output acknowledges that byte-deterministic re-runs are insufficient to validate a baseline's correctness, or explicitly distinguishes determinism checks from content-correctness checks
- Output does not treat passing hash/byte-equality checks as sole sufficient evidence that a VRT baseline is valid
